# Android 升级前备份与真实数据迁移演练（2026-09-08）

## 当前结论

[1d318bba 候选](HUAJIAO_ANDROID_CANDIDATE_2026_09_08.md)的安装前置核验取得新增证据：
手机身份匹配、实际已安装 APK 已备份并验签、关键状态取得两份一致快照，真实 schema 6 数据的
**本地生产迁移演练 1/1 通过**。**尚未安装候选，手机数据库没有升级，没有原生界面 PASS 增量。**

本批应用源码未改，新增可选探针提交 `63a4fe597b9a4440c8bc5842384d508203016faa`；
不重建原生代码和资源均未变化的候选。没有合并上游、更新依赖或发布。

## 设备只读检查与前台边界

- 显式 `-s 192.168.1.2:5555` 已连接，实际返回 `25102RKBEC / myron / Android 17`。
  在身份一致后才读取其他状态；没有操作备用设备或端口。
- 初次检查手机锁屏、最近活动为哔哩哔哩，Pure Live 无进程、无活动服务。没有唤醒、启动、
  force-stop、安装或输入；已询问可否切到 Pure Live 的测试窗口。
- 最后只读观察时手机已亮屏，前台仍为哔哩哔哩，活动记录与初次不同；不是本任务发出的切换，
  不推断变化来源，也不抢回 Pure Live。最后进程读取仍为 0，核心 DB/Hive 哈希与备份一致。
- 使用已有 Debug 包的 `run-as` 读取应用自身数据，无需调用 su、MT MCP 或 lspctl。
  没有修改常亮、网络、ADB、授权、Root/LSP/模块，也没有中断其他应用。

当前实际安装包为 3.1.8、Manifest 6121，备份文件 287,054,981 B，SHA-256：
`D965095AC696FC98D000E89542835F98C8A0C30DA0DF54EF4089D19571762E8D`。
该哈希与[历史 80b7431c 安装记录](TWITCASTING_COOKIE_ANDROID_RETEST_2026_09_07.md)一致，
不是新候选已上机。独立 apksigner 验证通过，证书为
`1e832295a696cf8210bca063e458bad0be12dfa64939d3362ff11b8f237ff7b9`，与 1d318bba 候选一致。

## 快照范围和一致性

2026-09-08 15:19:57–15:20:02 UTC，三次进程/服务检查均为空，两次独立 `exec-out run-as tar`
只读取得相同状态。**253 个归档条目、227 个文件**逐项路径、大小、mtime、模式和 SHA-256 一致，
两个完整 tar 字节哈希也一致；每份 **10,824,192 B**：
`0e2d97bf03abeabdea1f5abcc43422783b60ddfd57c0e7691e0aaaa3b6d6cdb5`。

范围包括当前/旧 Hive 设置、IPTV 缓存和 DB、证书、files、shared_prefs、no_backup（含 WorkManager
主库/WAL/SHM）和应用 WebView 状态。没有仅复制活动 WAL 数据库的主文件。原始归档不修改，
在本机独立副本中读取 SQLite，9 个数据库完整性均为 ok、外键检查均无违反。

这不是整机或整个 App 文件系统镜像：原有录制媒体、Flutter Debug 运行资源、旧下载 APK 和
可重建缓存未打入状态 tar；录制文件保持手机原位，当前实际安装 APK另行备份。原有用户配置和
登录数据只保存在本机忽略目录，不上传、不写入 Git，不在报告中展示来源 URL、Cookie 或节目内容。

实际 IPTV DB：`user_version=6`、4,980,736 B，主文件 SHA-256
`fd2a61d07482c15460e023c4b0f201b0d369399e8118213858a9d6046c7095d5`；当时没有该库的 WAL/SHM。

| 实际数据 | 数量 |
| --- | ---: |
| IPTV 来源 / 频道 | 1 / 143 |
| EPG 来源 / 频道 | 1 / 124 |
| 节目 / 映射 | 13,421 / 42 |
| 提醒 / 预约录制 | 0 / 0 |

提醒与预约在这份真实数据中为空；本次通过不新增非空用户动作的真实数据证据，相关非空/歧义回归
仍引用[既有迁移测试](EPG_SOURCE_IDENTITY_AUDIT_2026_09_08.md)。

## 本地生产迁移演练

新增 `tool/probes/epg_backup_migration_probe_test.dart` 默认跳过，仅在显式提供已核验本地快照与
输出目录时运行。探针先复制输入及存在的 WAL/SHM/journal，再在独立目录运行生产
`AppDatabase`，从不把原始快照交给 SQLite 写入，也不调用设备或网络操作。

1. 只在演练副本打开 schema 6，记录所有业务表及 sqlite_sequence 的完整字段快照。
2. 按文档合同独立构建期望：来源限定的频道主键、节目/映射引用和可唯一解析的用户动作引用。
   未调用生产身份函数生成期望；所有其余字段保持。对整表规范化 SHA 比较，避免失败日志泄露私有字段。
3. 实际生产代码升级至 schema 7，逐表完整内容与期望相同，完整性与外键检查通过。
4. 关闭并重开后仍为 schema 7、所有表指纹相同；原始输入各文件哈希保持不变。

**1/1 通过，探针文件 analyze 无问题。** 实际表中 124 个 EPG 频道、13,421 条节目、42 条映射
转换符合预期，143 个 IPTV 频道及来源设置等非目标字段保持。生产迁移打开耗时 **423 ms**，
这是 Windows 本机该数据副本的观测，不是 Android 安装、手机启动或大数据性能结论。

资源记录 `local-artifacts/build-records/20260908T153048453Z-phone-backup-local-rehearsal.json`：
338.286 秒，峰值 CPU 66.4%、工作集 12,558,667,776 B，结束活跃重型进程 0；签名、测试、分析
串行使用资源守卫，命令句柄退出 0。FFmpeg 测试 hook 未取得远端 SHA 的提示仍保留为跳过核验。
最终设备状态 JSON 曾因局部变量与 PowerShell `$Matches` 自动变量重名而序列化失败；修正本地记录
逻辑，复用已保存的只读焦点/哈希并重新读取进程后成功，没有因此重放任何设备输入。

## 下一步与验收状态

32 组候选原生场景仍全部 not-run；历史账本仍 **20 PASS / 32 RUN / 10 NR**。此次只是消除
备份可读、实际源版本和本地迁移合同的不确定性，不证明手机升级、界面、播放或录制成功。

- 可切换的 Pure Live 测试窗口确认后，重新核对设备/前台/录制、当前 APK 与 DB/Hive 哈希。
  输入变化时取得新快照；未变时可复用本次已核验备份，不把历史快照当成永久有效。
- 然后通过 NoRotation、显式 Serial 的共享包装器保留数据覆盖安装，核对实际 schema 7、
  数据关系及原始收藏/设置，再执行有边界的原生场景。回退 schema 6 应配套对应升级前快照。
- 界面窗口未确认期间继续本机源码/平台工作，不反复重建同一候选，也不更改其他应用前台。
- 全平台稳定 3.2.0、剩余 11 组参考平台、全部模式和性能验收仍待完成。

证据根目录：`local-artifacts/android-1d318bba-preflight-20260908/`，含原 APK、两个原始 tar、
成员哈希、数据库核验、签名对照、只读观察、演练副本和报告。该目录保持本地私有忽略状态。
