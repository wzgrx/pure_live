# 通用设置启动窗口尺寸弹窗审计（2026-09-11）

## 范围与基线

- 基线：`c38427149408773b3fa1c1bfd48f6cfcae768856`。
- 对应验收组：A2-01 的 Windows 二级设置页增量。
- 实际执行环境：Windows；视口 `320×480`、英文、3.0 倍文字。
- 操作链：通用设置长页 → 启动窗口尺寸 → 选择默认预设 → 检查宽高输入 → 取消。

## 修改前复现

干净红灯记录：
`local-artifacts/build-records/20260911T022514652Z-quality-focused.json`，结果 **1 PASS / 1 FAIL**。

- 窗口尺寸 `AlertDialog` 在窄屏大字号下向底部稳定溢出 **512 像素**。
- 英文界面的第一个预设仍显示硬编码中文“默认”，缺少完整本地化。
- 刷新率弹窗的前一项回归继续通过，说明本次问题限定在窗口尺寸编辑器。

测试准备阶段另保留一次夹具编译失败和两次格式门禁记录；它们未作为产品缺陷统计。

## 修订

- 窗口尺寸编辑器改为显式可滚动的 `AlertDialog`，标题、预设、宽高输入和操作按钮在受限高度内保持可达。
- 新增中英文 `default_option` 文案，第一个预设由当前语言动态构造，移除英文界面的中文硬编码。
- 保留五个预设、宽高输入、窗口调整行为、确认校验和取消不写入语义。
- Windows Widget 回归覆盖五个预设全部构建、默认预设实际点击后输入更新为 `1080×720`、宽高输入可滚动到达、
  取消关闭且已保存的 `1280×720` 保持不变。

## 验证

最终统一质量记录：
`local-artifacts/build-records/20260911T023015872Z-quality-focused.json`。

- 窗口尺寸/刷新率弹窗、窗口 PiP 几何、备份导入和设置首页布局共 **22/22 PASS**。
- 全库 `flutter analyze` 执行一次，**No issues found**。
- 中英文 JSON 可解析且键同步；仓库完整性审计错误为 0。
- 质量任务结束后重型进程为 0，实际 ADB 命令为 0。

## 当前边界

A2-01 保持 `RUN`。本批补齐 Windows 确定性窄屏/大字号和取消语义；真实 Windows 窗口管理器的确认调整、
多显示器边界，以及其余二、三级设置页仍按矩阵继续。本批未操作手机、构建候选、修改版本或发布 3.2.0。
