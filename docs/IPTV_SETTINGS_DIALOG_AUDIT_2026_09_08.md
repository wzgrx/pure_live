# IPTV 设置弹窗生命周期与布局（2026-09-08）

## 范围与来源

基线 fdc8861471031f611aba3eea7b2cbe9b8353c6af，修复 `759788975eac4ca0034ee44ede2a7b214f6df870`。本批审查 IPTV 设置中的 UA 编辑、同步间隔和 EPG 导入标签，没有宣称导入、数据库同步、播放或系统文件选择器整体通过。冻结上游 `c6c9bd70aedc503c003110dae10a83ad0bb891d8` 中同样存在提前释放控制器、非响应式 UA 副标题和固定弹窗 Column，且中英文均缺 `import_epg_source`；分类 **upstream-existing**。只读本地冻结对象，无 fetch/merge。

## 稳定复现与修复

| 问题 | 第一个错误状态 | 修订 |
| --- | --- | --- |
| 输入 UA 后取消/确认出现已释放控制器 | showDialog 的结果 Future 已完成，但退场动画中的 TextField 仍挂载；whenComplete 已 dispose | 草稿控制器和高度 Rx 由独立 Dialog State 持有，仅随子树卸载释放；返回 String 后检查页面 mounted，再提交和提示 |
| 保存后设置页仍显示旧 UA | 普通 buildTile 读取 Rx，不订阅更新 | 局部 Obx 订阅；使用完整源字符串交给现有 tile 约束显示，不按30字符预截断 |
| UA 弹窗窄屏大字体溢出179 px | 320×480、2倍字体，说明文字加固定输入高度超过剩余空间 | AlertDialog 可滚动，保留字体缩放、全部说明、动作和80～350高度拖拽 |
| 同步间隔弹窗同条件溢出329 px | 六个选项的 Column 没有滚动能力 | 使用对话框滚动内容，所有2/6/12/24/48/72小时选项仍可达 |
| EPG导入显示资源键 | 两个翻译文件缺现有入口使用的键 | 添加“导入节目单源”及“Import EPG source”，不改现有导入实现 |

取消、遮罩和系统返回不保存草稿；确认空白字符串仍是显式清空而非取消，其他输入继续 trim、500字符上限。同步开关、后台启动策略、存储字段、播放器/录制器、网络和数据库逻辑未变。回滚上述页面和翻译会恢复已复现问题，没有数据迁移。

## 红绿证据

- 首次红测记录 `20260908T044419974Z-iptv-settings-page-red.json`，首个释放异常有效，但失败测试未卸载页面污染后续 Get 根容器；不把后续夹具异常当产品根因。
- 修正夹具为失败时也卸载页面后，`20260908T044540695Z-iptv-settings-page-red.json` **4/4稳定失败**：控制器释放、保存副标题缺失及179/329像素溢出。生产源码此时未改。
- 初始修复4项通过，记录 `20260908T044737997Z-iptv-settings-page-green.json`；其通用 green 日志后被扩展运行更新，不再作为独立原始日志索引。
- 扩展首次9/12，记录 `20260908T045022690Z-iptv-settings-page-green.json`：两项误用 tester.pageBack 寻找不存在的可见返回按钮，一项英文长页 ensureVisible 后未泵帧导致点击旧坐标。改为实际系统返回事件及滚动布局提交后点击，保留失败日志；没有改变生产行为来迎合夹具。
- 最终页面 **12/12**，记录 `20260908T045205427Z-iptv-settings-page-green.json`。包括取消/保存、即时外部更新、空值重置、遮罩/系统返回/重开、所有间隔选项、中英文键盘内缩180像素、2倍字体仍保持、输入高度拖拽与翻译入口。
- 最終正式定向门禁五文件 **19/19**，`20260908T045754234Z-quality-focused.json`；另包括 IPTV 启动策略、资源加载门禁、播放画质与设置生命周期。全量 analyze 一次 **173.7秒无问题**。332.129秒含等待其他Java工作的时间，峰值 CPU79.8%、14,837,882,880 B，结束活跃重型进程0。

所有页面测试使用真正 IptvPage、翻译、Hive/Rx 和弹窗，Hive是临时目录、数据库用隔离内存连接/受控列表读取。未打开真实DB、下载节目单、调用网络或设备。没有吞掉FlutterError或关闭字体缩放。

## 仍待完成

- 同页网络导入表单也有在结果 Future finally 释放两个文本控制器的相似实现；尚未在本批复现和修订，下一批优先验证其取消/成功/失败/迟到响应以及请求互斥。
- EPG来源选择在数据库读取后打开对话框，加载/空态/错误和退出后迟到结果需要继续建立真实页面对照。
- Android候选96673538及Windows候选f3de664a均不含本批设置修订。本次未新构建/安装，不重复旧候选全量门禁来冒充新业务代码验证；完成相邻设置/导入修复批次后再累计打包。
- 无手机/ADB/MT、Root/LSP、数据清理、重启或发布。历史62项台账仍20 PASS、32 RUN、10 NR；本次源码测试不变更原生大项状态。全平台3.2.0继续未完成。

本机证据：`local-artifacts/iptv-settings-page-20260908/`，保留有效红测、夹具失败、最终页面/聚焦日志、冻结上游页、源文件哈希与终态检查点。
