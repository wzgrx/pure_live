# Android 新候选观看与录制验收（2026-09-06）

## 候选身份与执行范围

- 源码 `c252e522`（应用代码与 `7ba627fd` 一致）；完整质量证据与 APK 哈希见 [构建审计](ANDROID_BUILD_PATH_AUDIT_2026_09_06.md)。开发包 3.1.8+4121，Manifest code 6121，不是正式 3.2.0。
- `adb install -r` 成功，保留现有数据。网络 transport `192.168.1.2:46463`，K90 Pro / 25102RKBEC，Android 17 / SDK 37。设备实际页大小 4096；16 KB ELF/ZIP 兼容是构建检查，不冒称在 16 KB 内核上运行。
- 两个独立轮次均使用 `run_android_device_test_turn.ps1 -NoRotation`，唤醒和前台守卫启用、finally 恢复常亮设置。录制测试结束移除本次创建的监控；没有删除历史记录。

## 斗鱼实际观看、选源与录制

证据目录：`local-artifacts/diagnostics/android-douyu-c252e522-20260906/`，08:40:33–08:43:12，房间 pigff / 24422。

- 实际游戏视频呈现。画质由原画1080P60切到超清，提交用时 3175 ms；线路1切到线路2，用时 3166 ms。后续观察标签稳定，截图 `line-switch-committed.png` 同时显示画面、超清、线路2与实际弹幕内容；不是只检查菜单存在。
- 新录制默认独立选择原画1080P60 / 线路1，停止后的录制卡片准确保留该档位；它不继承观看的超清 / 线路2。
- 运行文件由 5,505,024 增长到 8,126,464 字节。请求短录20秒，停止操作时墙钟25.225秒；完成 MP4 媒体时长33.526667秒，19,460,622字节，H.264 1920×1080/60fps + AAC。
- SHA256 `88652a95bf6beb55c5620a766b19bb0af111119591c35d7b1352ac5d4c14ec5d`。独立 FFmpeg 严格全文件解码退出0、错误日志空，2.672秒；证据 `recording-full-decode.json`、`recording-decode.log`。这不替代扬声器听感验收。
- 最新卡片已停止、无新失败；可见的9月4日历史YY失败卡片原样保留，不把旧失败归因本轮。无捕获到的致命异常；测试清理 force-stop 后进程和唤醒锁消失，不能据此声称普通返回自动退出进程。
- `ScreenOffSeconds=0`，脚本汇总里的 screenOff 条件是未请求时的条件通过；本轮没有锁屏录制验收。前台干扰记录1次并由守卫恢复1次，不计为全程无干扰。

## Bilibili 纯音频与系统 PiP

证据目录：`local-artifacts/diagnostics/android-runtime-c252e522-20260906/`，08:44:18–08:46:18。

- 冷启动、热门列表刷新及进入沐泽三角洲行动房间成功；没有16 KB兼容警告。
- 纯音频提示出现（3626 ms），切回视频提示清除（6438 ms），后续截图呈现实际画面。
- Activity 状态确认 Pure Live 进入系统 PiP，`system-pip.png` 小窗有画面；重新前台后 `room-after-pip.png` 房间与视频恢复、原画/线路1标签可见。系统 PiP 下的其他前台应用不作本项目操作对象。
- 弹幕服务器连接正常；此房间截图只有系统连接消息，未把它计为新实际弹幕消息通过。无日志致命异常；最终测试 force-stop 清理完成。
- Debug 观察快照：PSS 781,814 KB，CPU 12%，81线程；gfxinfo仅14帧，其中1帧janky。样本小且包含调试/切换开销，不构成Release性能验收，不据此宣称流畅度达标。

## 剩余

本轮闭合新 Android 候选的普通播放选源、短录、纯音频和系统PiP基础往返。Windows长暂停恢复与UI响应仍待新候选；Android应用内小窗/快速重选/终态重试、长暂停与后台长录、国外平台及更多界面操作继续按完整清单验收。未合并上游，未发布正式3.2.0。
