# EPG 来源选择加载、退出与大字体（2026-09-08）

## 修复与来源

基线6efab919eee077fa958f64174c392b10bfd230c6，修复 `98528ae118492d936c8992a4f793e48ad5bc1467`。接续 [IPTV网络导入审计](IPTV_NETWORK_DIALOG_AUDIT_2026_09_08.md)。冻结上游 `c6c9bd70aedc503c003110dae10a83ad0bb891d8` 的同一方法也先await读库、捕获错误后仍显示空列表，再通过Get.dialog打开；标题同样是没有Expanded的Row。归类 **upstream-existing**，未查询“最新”或合并上游。

稳定红测证明四个问题：请求期间没有窗口/加载反馈；查询失败显示成“未找到来源”；离页后迟到查询仍调用打开弹窗；320×480、双倍字体标题横向溢出64像素。

修订为立即打开的独立Stateful弹窗：

- 弹窗持有读库Future的展示状态，成功/失败先检查自身mounted；页面退出/取消不让旧结果创建新窗口。保存不可变结果列表，无整场累积或额外轮询。
- 页面防重入标志与弹窗读库标志各自保护重复打开/重复重试；一次请求未结束前不另发相同读取。失败独立文案与“重试”；真实空结果仍显示原来的空态，不伪造来源。
- 行点击只返回实际EpgSource；页面在结果返回后且仍mounted才保存同一来源的id/name。加载、关闭、取消和失败不写选择。
- 标题Expanded给关闭按钮留位；来源列表是唯一主要滚动区域，不把整个列表再套外层对话框滚动。高度受当前窗口约束，长名称保留换行，URL保持既有单行省略。新增中英文读库失败文案和关闭按钮tooltip。

未改变数据库结构、默认来源初始化、节目单解析/同步、播放、网络导入流程或Root模块。UI取消只放弃展示，不宣称中止SQLite查询。回滚上述页面/双语文案会恢复已证明缺陷，无数据迁移。

## 验证和夹具纠错

- `20260908T052815908Z-iptv-epg-selection-red.json`：原21项通过，新增4项均失败，对应上述四个根因。未使用真机、HTTP或真实用户数据库。
- 初始25项通过，`20260908T053152151Z-iptv-epg-selection-green.json`；补充重开/迟到结果、重试互斥、异步异常、20条长名称以及真正选中id/name后继续扩大测试。
- 两轮扩展29/31，`20260908T053419686Z-iptv-epg-selection-green.json`、`20260908T053735999Z-iptv-epg-selection-green.json`。一次从外层+列表双滚动简化为单列表，但失败依旧，因此未把该设计调整冒称为失败根因修复。
- 诊断 `20260908T053915017Z-iptv-epg-selection-red.json` 记录实际offset8000，而长列表最大约9003.733/9035.733；50次手势耗尽仍在第17/18项，并非滚动停滞。按20条实际换行尺寸把测试有界手势容量扩到80，未缩短列表/字体。
- 随后30/31，`20260908T054045841Z-iptv-epg-selection-green.json`：英文全局文字finder误匹配前一测试留下的页面副标题，点击到了弹窗Cancel区域。修正为弹窗ListView内的文字，并为每例显式重置来源名称；没有绕开命中检查或直接调用选择回调替代长列表实际点击。
- 最终页面 **31/31**，`20260908T054227324Z-iptv-epg-selection-green.json`。含4个原红项、重开时旧成功/旧失败隔离、重复打开/重试只读一次、中英文320×480/2倍字体的第20项实际滚动选择，保留全部前批UA/网络导入测试。
- 五文件聚焦 **38/38**、全量analyze一次320.3秒无问题，`20260908T055028320Z-quality-focused.json`；465.403秒，峰值CPU75.09%、15,603,630,080 B，结束活跃重型进程0。其他Java活跃时按守卫排队，未终止其他任务。
- 本轮FFmpeg预取校验通过，但测试hook另报远端SHA缺失并跳过；保留原记录，不把该hook算作远端校验通过。未升级SDK/依赖或原生库。

测试运行真正IptvPage、对话框导航、临时Hive和内存DB/受控查询；是Widget与行为证据，不是Android/Windows原生UI或节目单下载解析证明。31项与前批21/12项存在包含关系，不简单累加。

## 当前交付与剩余

本批未构建/安装，Android96673538、Windowsf3de664a不含三批IPTV页面修订。下一阶段从新的干净提交构建累计Android候选，执行新源码完整门禁及APK内容/签名/哈希核验，再按当前设备窗口推进原生。旧2098项全测不用于证明新业务源码。

IPTV初始化失败/默认源导入、管理页本地和网络真实导入/编辑/删除、节目单解析与数据库一致性、原生文件选择器继续待验。来源列表打开后外部删除/禁用来源的跨页面同步也尚无本批证据。无ADB/MT/手机、重启/清数据/模块更新或发布；历史62行仍20PASS/32RUN/10NR，全平台3.2.0目标继续。

本机证据：`local-artifacts/iptv-epg-selection-20260908/`，包含所有红绿及失败纠错、几何诊断、最终日志、冻结上游页、哈希索引与终态checkpoint。
