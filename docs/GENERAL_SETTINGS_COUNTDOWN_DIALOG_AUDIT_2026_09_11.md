# 通用设置倒计时弹窗布局与输入生命周期审计（2026-09-11）

## 范围与基线

- 基线：`4b9e6e44371d5489ddf333df588f250bbc82bc65`。
- 对应验收组：A2-01 的通用设置操作增量，A2-03 的自动退出配置呈现子项。
- 固定视口：`320×480`、英文、3.0 倍文字。
- 操作链：通用设置长页 → 退出前等待时间 → 选择 15 分钟 → 再次打开 → 输入 7 分钟 → 保存。

## 修改前复现

干净红灯记录：
`local-artifacts/build-records/20260911T023405173Z-quality-focused.json`，结果 **2 PASS / 1 FAIL**。

- 倒计时 `AlertDialog` 在窄屏大字号下向底部稳定溢出 **548 像素**。
- 红灯中的“120 min”重复匹配同时来自页面摘要与弹窗预设，后续将断言限定在弹窗子树；它未计作产品缺陷。
- 将内容改为有界滚动后，继续执行自定义输入保存，稳定暴露 `TextEditingController was used after being disposed`：
  旧代码在 `showDialog().whenComplete(...)` 中立即释放控制器，而反向路由动画中的已聚焦 `TextField` 仍会重建。

## 修订

- 倒计时弹窗改为整体可滚动布局，说明、七个预设、自定义输入与操作按钮在受限高度内保持可达。
- 抽出私有有状态弹窗，由其 `State` 创建并在真实卸载时释放 `TextEditingController`，不再由路由结果提前释放。
- 保留七个固定预设、任意正整数分钟、自定义输入校验、即时保存和关闭弹窗语义。
- Widget 回归先实际选择 15 分钟，再重新打开、聚焦输入框、输入 7 分钟并保存；两次状态提交和弹窗关闭均需成功，
  且全程无布局或生命周期异常。

## 验证

最终统一质量记录：
`local-artifacts/build-records/20260911T023725885Z-quality-focused.json`。

- 倒计时/窗口尺寸/刷新率弹窗、备份导入和设置首页布局共 **17/17 PASS**。
- 全库 `flutter analyze` 执行一次，**No issues found**。
- 仓库完整性审计错误为 0，质量任务结束后重型进程为 0，实际 ADB 命令为 0。

## 当前边界

A2-01 保持 `RUN`，A2-03 保持 `PASS`。本批为确定性 Widget 交互证据；实际后台计时到期、系统退出、
Android 生命周期和 Windows 当前候选操作仍按原验收记录与最终候选门禁执行。本批未操作手机、构建候选、
修改版本或发布 3.2.0。
