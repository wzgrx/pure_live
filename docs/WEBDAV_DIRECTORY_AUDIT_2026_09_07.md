# WebDAV 目录与异步状态审计（2026-09-07）

## 范围与来源

- 本仓库基准：`7dcb7001`；没有合并上游。
- 只读对照本地冻结的 `upstream/master`：`c6c9bd70aedc503c003110dae10a83ad0bb891d8`，不声称这是查询时的远端最新提交。
- `webdav_service.dart` 的空列表抛错及 `debug: true` 与上述冻结上游一致，分类为 **upstream-existing**。不是此前恢复等待写盘修改引入。
- 依赖为锁定的 `webdav_client 1.2.2`。其 `readDir` 先检查 PROPFIND HTTP 207，再解析 XML；正常响应里的目录自身被跳过，因此只有自身的合法目录返回空列表。

## 第一个错误状态

`WebDAVService.readDirectory` 在依赖已经成功解析后，对 `response.isEmpty` 抛 `Empty response from server`。新建目录没有子文件是正常状态，页面却进入目录加载失败分支，用户首次上传备份前即看到错误。非空目录、HTTP 错误和 XML 语法错误需要分别验证，不能把所有失败变为空列表。

同时，服务构造固定开启依赖调试日志。依赖 `WdDio` 在 debug 模式添加 `LogInterceptor(responseBody: true)`，并在请求选项中设置 authorization。默认记录请求头和响应体可能带出凭据与恢复内容；日常备份不需要这样的协议明文日志。

## 独立待处理的相邻风险

以下是代码审查发现，尚未写成“已稳定复现/修复”的结论：

1. `loadFiles` 没有请求代次或服务/路径快照约束，先发请求后返回时可能覆盖新目录或新配置的列表，旧失败也可能覆盖新成功。
2. 删除当前配置后 `initializeWebDAV` 的 null 分支不清理旧服务、列表、加载/错误状态；`_webdavService` 在首次无配置时又是未初始化的 late 字段。
3. 下载恢复没有跨网络等待的选择身份约束；切换配置/离页后，先前下载仍可能提交本地恢复。上传、删除的完成反馈和刷新也没有对应所有权约束。
4. 空目录目前没有明确空状态文字。修正服务返回后还需检查目录页面的空状态、上传入口、窄窗口和大字号呈现。

这些项目保留为 WebDAV 工作组的未验收项，不用一个目录解析回归代表整个云端备份通过。真实用户云端配置、上传和删除本轮均未操作；本地 HTTP 夹具只监听 loopback，使用合成认证值。

## 服务层复现、修复与验证

本地 HTTP 服务器返回真实 PROPFIND 207 XML。修改前 3 通过 / 2 失败：合法“仅目录自身”响应被服务抛错，协议 debug 标志保持开启；非空文件元数据、403 和不完整 XML 的失败边界已分别检查。红基线 `20260906T172458324Z-quality-focused.json`。

修复只涉及服务包装层：直接返回已完成 HTTP 状态检查/XML 解析的 readDir 结果，移除空列表报错和无意义 catch/rethrow；构造客户端固定 `debug: false`，不改认证算法、HTTP 状态、下载恢复、代理或配置存储。

增加 Basic challenge → 带认证重试的 loopback 用例，确认关闭协议日志后认证仍成功。最终服务 6 项与完整备份往返 6 项共 **12/12** 通过，analyze 无问题，记录 `20260906T172839321Z-quality-focused.json`。此处是实际本地 HTTP + 锁定依赖解析器证据，不是远端云盘或原生页面验收。回滚点为 `7dcb7001`；上节异步状态和空页面呈现仍待下一批处理。

## 原生确认：无配置刷新破坏初始页面

上述审查项 2 的“首次无配置”分支现已在 `cb0082e1` Windows Debug 实际复现：设置 → 备份与恢复 → WebDAV，初始正确显示暂无配置和创建入口；更多操作 → 刷新后，页面持久显示 `_webdavService` 未初始化的 `LateInitializationError`，创建入口被错误页面替代。此时未添加任何配置，没有外部 WebDAV 请求或云端操作。

第一处错误仍在 `loadFiles` 对未初始化 late 服务的访问；`errorMessage` 又优先于无配置分支显示，使可恢复初始界面变成内部错误页。不是空目录解析修复导致，冻结上游控制器具有同一访问路径。证据 `local-artifacts/backup-first-run-native-20260907/webdav-no-config-refresh.json` 和该轮原生窗口截图/树记录。

下一批应先通过真实页面/控制器回归锁定无配置刷新，再覆盖配置清除、目录/配置请求乱序及离页后的迟到响应；本批尚未修复这些控制器状态问题。该候选的完整 1223/42 门禁通过，仍遗漏此 GUI 场景，故不得把自动门禁当成全功能完成证明。

## 后续修复：目录与传输的服务所有权

以 `8389b247` 为回滚点，控制器先加可注入服务工厂，保留旧行为运行六个确定性用例。**0/6 通过**，分别复现无配置刷新、旧目录覆盖新目录、旧请求提前结束加载、清除配置残留旧列表、旧账号覆盖新账号、离页后继续发布结果。红记录 `20260906T174843099Z-quality-focused.json`。这些目录根因与冻结上游一致，分类沿用 `upstream-existing`；不涉及上游合并。

本次采用局部生命周期修复：

- 服务改为可空，由页面拥有；替换、清除配置或关闭页面时先失效代次，再关闭旧客户端。无配置刷新保持正常初始状态，不产生内部异常。
- 每次目录请求捕获服务身份、服务代次、请求代次和目录路径。仅当前请求发布列表、错误与加载结束；清空旧列表，阻止旧目录项目在新目录下被继续操作。
- 当前加载失败由页面持久错误和重试按钮承载，去掉独立的目录错误 snackbar；过期失败不覆盖新页面。正常空目录显示已有本地化空状态。
- 面包屑按实际路径重建，返回祖先目录后移除失效后缀。创建/编辑配置先更新选择再持久化；未选择配置时上传入口置灰，目录项目不显示恢复操作。
- 上传、删除和下载均捕获创建请求的服务。删除确认返回后再次核对服务和目录，防止在新账号上执行旧确认；迟到的上传/删除结果不刷新新服务。下载在网络返回后、进入本地恢复之前核对所有权，避免清除配置、切换服务或离页后启动恢复；保持等待现有持久化流程完成再反馈成功。

服务新增的 read/write/remove 包装只委托锁定客户端；没有改 HTTP 认证和路径协议。关闭连接可能中止在途传输，但不承诺撤销服务器已经接受的写入/删除；本地恢复一旦进入提交阶段继续完成持久化，离页仅抑制其后续界面反馈，不做半途回滚。

首轮 **16 个控制器用例 + 6 个真实 loopback 服务用例 = 22/22** 通过，记录 `20260906T175533558Z-quality-focused.json`。新增真实 WebDavPage 的 360×640 Widget 覆盖 **5/5** 通过，记录 `20260906T182114209Z-quality-focused.json`：菜单无配置刷新、正常空状态、错误重试、清除配置恢复入口、下载等待本地恢复。该轮不操作用户云盘；传输时序测试使用内存服务，真实持久化沿用备份往返用例另测。

Widget 夹具迭代曾因遗漏 Hive Rx 扩展导入而未通过 analyze，之后补齐加载控件需要的 ThemeSettingsController。最后一个用例还把恢复 Completer 建在 setUp 的根时钟中，混合了真实微任务与 Widget 假时钟，成功提示断言及清理出现挂起。修正为在 restoreAllSettings 被调用时创建 Completer、通过 pump 推进并记录完成状态，保留“恢复前无成功提示、恢复后确实可见”的断言。失败轮次只停止经父命令和工作区验证的本轮 flutter_tester 叶进程，由质量脚本正常记录失败并释放锁；未停止其他项目任务。正式用例移除了临时诊断输出。

最终合并复验 **33/33** 通过（控制器 16 + 页面 5 + loopback 服务 6 + 真实 Hive 备份往返 6）；analyze **无诊断，139.3 秒**。记录 `20260906T182459903Z-quality-focused.json`，总耗时 210.107 秒，结束活跃重型进程 0。该记录对应以 `8389b247` 为基线的本批修复工作树，不是旧提交自身的行为；验证后仅补充证据文档。

当时完整门禁、新原生产物及对应 GUI 复验仍待补证，结果见下节。旧 `cb0082e1` 的 1223/42 与 Windows 包只证明旧输入，不计入本次修复验收。WebDAV 更完整的服务器兼容性、配置表单、大字号与传输交互仍保留待验收项。

## 完整门禁、Windows 构建与原生补证

源码 **`b5ab680db8c108e9ad1a55f7a6868d93e94bbd6a`**，干净工作树，ADB 无设备，因此执行 Windows 阶段，没有同步上游或发布版本。

- 完整门禁：**1244/1244 测试 + 42/42 接口探针**，analyze 无诊断（104.2 秒）；`20260906T183447740Z-quality-full.json`，454.870 秒。
- Windows x64 Debug：`20260906T183848021Z-build-windowsx64-debug.json`，全流程 718.509 秒，Flutter 编译 114.5 秒；构建结束活跃重型进程 0。已有 CMP0175/MSB8028 警告仍保留，未通过清理全部缓存掩盖。
- 版本仍为开发候选 `3.1.8+4121`，SkipInstaller，没有新 setup.exe 或正式签名。ZIP `local-artifacts/3.1.8-4121/PureLive-3.1.8-4121-windows-x64-debug.zip`，**141,245,729 B**，SHA-256 **`29030ACE86B39C01625738F9484CF5D831DEC7ABC1D39FD908DB3A403E345E54`**。
- Debug Dart 代码身份以 `data/flutter_assets/kernel_blob.bin` 为准：SHA-256 **`700F578B52FB2D93CD1F6A04F2A76EAD73A1624CFB10C533FABF28D0F2D2440F`**。runner EXE 哈希与旧候选相同，不单凭 EXE 推断本次代码。
- 旧 `cb0082e1` ZIP 与元数据已复制到 `local-artifacts/candidates/windows-cb0082e1-debug/`，前后 SHA-256 一致；旧包是故障对照，不被新证据覆盖。

实际原生窗口 1276×718，先用全新候选便携配置重复此前的最短复现，再在应用退出后将同一测试配置置入仅监听 loopback 的无认证目录夹具。没有输入用户云盘凭据，夹具只实现 PROPFIND；上传、恢复、删除均未执行。

| 原生操作 | 观察和协议证据 | 结果 |
| --- | --- | --- |
| 无配置 → 更多操作 → 刷新 | 创建新配置入口仍在，没有 LateInitializationError | PASS |
| 打开夹具根目录 | PROPFIND `/` → 207，列出 `backups` | PASS |
| 依次进入 `backups`、`empty` | `/backups/`、`/backups/empty/` 均 207；合法空目录显示“暂无数据” | PASS |
| 点击“我的文件”返回根目录 | 根目录重新 207；旧 `backups / empty` 面包屑后缀消失 | PASS |
| 夹具切为 503，再刷新 | 根目录 503；列表被当前错误页替代，保留“重试” | PASS |
| 夹具恢复 207，点击“重试” | 第六个实际请求根目录 207；错误消失、`backups` 恢复 | PASS |
| 打开目录项目菜单 | 仅出现删除，没有同步到本地；关闭菜单，未执行删除 | PASS |

证据目录 `local-artifacts/webdav-native-20260907/` 包含候选哈希、无配置结果、6 条真实请求 NDJSON、目录结果、可复用的目录服务与配置生成脚本，以及归档的 `candidate-AppData`。窗口截图和 UI 树保留在该轮任务工具记录中；瞬间 UI 树曾落后于画面，重取稳定状态后才确认结果。

收尾通过正常退出关闭应用，校验无 Pure Live 进程后把本轮 AppData 移至证据目录，运行目录恢复为无测试 AppData；只停止了命令行明确匹配本轮脚本的 Python 夹具进程。没有删除用户数据、改变系统设置或操作其他项目任务。

这些结果关闭了本批 Windows 目录浏览缺口，不代表 WebDAV 所有操作或整个 3.2.0 已验收。真实服务器差异、表单/目录名边界、上传下载删除的完整客户端交互、Android 新候选、大字号及其他功能/长时平台矩阵继续；当前错误文案仍直接展示底层异常，可在后续 UI 一致性批次中优化。
