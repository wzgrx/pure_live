# WebDAV 已保存选择审计（2026-09-07）

## 基线与来源

- 源码基线 `607f377e`。只读对照冻结上游 `c6c9bd70aedc503c003110dae10a83ad0bb891d8`，双方 `onInit` 都直接反序列化保存的完整选择对象，不处理 JSON 异常，也不与配置列表核对：**upstream-existing**，由旧数据或损坏数据触发。本轮没有合并上游，也没有查询远端最新状态。
- 最短复现：临时 Hive 写入损坏的 `currentWebDavConfig` 后创建页面控制器；或列表为空但选择仍保存旧配置；或同名列表项地址已更新、选择快照仍旧。第一处错误发生在初始化选择的信任边界，而非目录请求返回阶段。

## 红测与修复

- 新增 9 项在旧实现上全部失败，既有 16 项通过。记录 `20260906T191302561Z-quality-focused.json`，日志 `local-artifacts/webdav-selection-red-20260907.log`。
- `{broken` 抛 FormatException；`null`、数组、数字及非字符串 name 抛类型错误；空对象和孤立选择被当作可连接配置；同名旧快照覆盖列表对象；非法已存地址仍创建服务。
- 页面现在仅从选择快照提取非空名称，解析成列表中唯一的同名对象。列表的地址、用户名与密码为连接依据；匿名配置仍兼容。失配、歧义或损坏选择不启动服务。
- 打开页面不重写原始选择，也不删除或重建配置列表；显示通用提示及创建/选择入口，不展示原始 JSON 或凭据。显式选择或保存后沿用既有持久化行为。
- 已保存地址复用表单格式验证。自动恢复失败时保留列表编辑入口；显式重选非法地址时显示持久错误，刷新不把它清成成功空目录。
- 无配置/未选择页面采用可按内容增长的 sliver，提示在小窗口大字体下仍可滚动到操作入口。

## 验证状态

- 首次修复后 **52 通过 / 2 失败**（`20260906T192215857Z-quality-focused.json`）：控制器新增用例全绿；新页面夹具在本地化加载前创建控制器，暴露了提前翻译并缓存 key 的问题，另一个页面失败为前一失败未完成页面卸载后的连带状态。改为保存提示 key、页面渲染时翻译；同时去掉测试中对同步 `Get.delete` 的多余 await（analyze info）。不把该夹具时序夸大为已复现的原生启动故障。
- 最终 **54/54 通过**：配置模型 2、真实页面 13、目录/选择/传输状态 27、loopback 服务 6、真实 Hive 备份恢复 6。覆盖重复名称歧义、快照非名称字段损坏、Hive 原文保留，以及页面提示、创建入口、显式重选恢复；320×480 / 2 倍文字用例通过。
- analyze **No issues found，168.3 秒**；门禁记录 `20260906T192756440Z-quality-focused.json`，日志 `local-artifacts/webdav-selection-final-20260907.log`。源基线 `607f377e` 加本批工作树；总耗时 254.936 秒，`active_heavy_processes_after=1`，不声称系统所有重型进程为零。
- 格式化曾等待另一项目 Gradle 测试自然结束；未停止其他项目进程。一次格式化包装命令的释放参数拼错导致退出码 1，随后以正确 `-Lease` 重跑（0 changed）并正常释放；最终门禁为独立成功记录。

## 边界与下一步

- 未调整全局 Hive 列表解析或备份迁移结构。若整个列表存储损坏，已有全局解析器仍回落为空列表；本批只保证页面不根据遗留选择连接，完整列表修复另行审查。
- 未加入打开页面后的外部设置双向订阅；上传根目录限制、传输去重/大小边界及实际服务兼容继续验收。
- 这批源码尚未构建或做原生验收；已有 `b5ab680d` Windows 目录验收不覆盖本改动。3.2.0 未发布。
- 回滚可撤销本批控制器/页面/翻译与相应测试变更；存储格式没有迁移，用户原始数据没有清理。
