# 分段时钟原生回归准备与解析合同（2026-09-10）

问题与离线方案见 [拼接时钟定位](RECORDING_CONCAT_CLOCK_DRIFT_2026_09_10.md)。本检查点增加自生成素材原生探针及有界时钟解析器，尚未修改生产录制参数或接入生产收尾。

## 确定性素材与探针

- 素材由 testsrc2 与 sine 生成，各 26 秒，不使用真实用户媒体或网络流。
- 四组：H.264 B 帧固定帧率 + AAC、保留源时间空隙的 VFR + AAC、纯视频、纯音频；有音视频的组包含音频起点偏移。
- 每组对照生产单段、生产旧多段、候选多段路径。录制/收尾通过项目实际 FFmpegManager/FFmpegKit 执行；候选时钟清单暂在探针内构造，生产收尾另行接入。
- 外部 ffmpeg 只承担素材生成和解码观测；逐帧内容顺序、容器起点常量偏移与新增时钟阶跃分开判断。旧路径必须在固定帧率样本实际复现偏移才算完成复现。
- 输出、TS、CSV、原生事件、逐帧哈希与失败报告全部留在 `local-artifacts/recorder-clock-native-20260910/`，测试不会删除这些证据。

## 第一轮失败保留

`20260910T145720735Z-recorder-clock-native-first.json`：10/10 帧哈希解析测试通过，三文件严格分析通过，四组生成成功；原生 0/4。
失败发生在探针把有限本地文件作为 liveRecording=true，然后错误地期待自然 EOF 得到 complete。
现有 `FFmpegTerminalDecision` 明确把未请求的直播 EOF 视作可恢复错误，即使退出码为 0；这与时钟缺陷无关。
探针已使用有限文件模式，保留同一套生产录制参数，不修改直播恢复规则。原生 DLL 采样后指纹仍与先前锁定值一致。

## 解析与定向验证

新增 `FrameHashTimeline` 的 10 项测试覆盖常量偏移、分段阶跃、VFR、丢失/重复/重排、不同有理时间基和格式错误。
新增 `RecordingSegmentClock` 的 22 项测试覆盖真实 CSV 起点、非等长/重排帧末尾、单段、未完成末行、错序/外来/缺失分段、路径与数值、CRLF、引号路径和 8 MiB 有界读取。
解析器按相邻参考流起点差计算清单 duration，不把前一段的估算结束时间当下一段起点；使用显式 inpoint 0。
它仅定义候选 clock-v1 的合同，旧 TS 的时钟语义不同，尚未自动应用于任何录制。

修正后合并 **32/32** 定向通过，五个修改 Dart 文件严格分析无问题。
对应日志前缀为 `20260910T145814334Z-finite-source.log`。

## 有限输入原生终态与精确复核

`20260910T151213132Z-recorder-clock-native-finite-source.json` 保留原始 **1/4 通过、3/4 失败**：四组共执行 24 个实际 FFmpegKit 录制/合并会话，退出码均为 0，外部逐轨解码退出码为 0 且 stderr 为空。
原生日志仍包含 `pthread_join ESRCH` 和纯音频 TS 的 `frame size not set` 警告；上述退出码与内容证据不代表所有原生日志无警告。

三项失败都落在音频“两采样点”门槛：先把大 PTS 换算成 double 再相减，使恰好 `2 / 44100` 秒的偏移范围多出约 10⁻¹⁵ 秒。
现将帧时间保留为整数 ticks 与有理时间基，用 BigInt 完成跨时间基偏移及最大最小值相减，约分后才转换为秒。
阈值保持视频 `2 / 90000` 秒、音频 `2 / 44100` 秒，没有增加 epsilon；新增测试同时验证两采样点边界通过、三采样点超限。

`recorder_clock_retained_probe_test.dart` 逐文件核对四组原始 summary 与 framemd5 的 SHA-256，然后对同一原生输出重新判定，**4/4 通过**。复核写入独立 `exact-clock-verdict.json`，原来的失败 summary 和构建记录保持原样；这不是第二次原生录制。
合并定向测试为 **37/37**（11 项帧时钟、22 项分段时钟解析、4 项留存证据复核），三个修改 Dart 文件严格分析无问题。
日志为 `20260910T151552986Z-first.log`，终态记录为 `20260910T152150196Z-recorder-clock-exact-first.json`。

| 样本 | 解码帧数（视频 / 音频） | 旧多段相对单段的偏移范围 | 候选相对单段的偏移范围 |
| --- | --- | --- | --- |
| av_cfr | 520 / 1121 | 视频 176.844 ms，音频 176.848 ms | 视频 0，音频 2 个采样点 |
| av_vfr | 506 / 1121 | 视频 174.822 ms，音频 174.830 ms | 视频 0，音频 2 个采样点 |
| video_only | 520 / — | 视频 0 | 视频 0 |
| audio_only | — / 1121 | 音频 510.884 ms | 音频 2 个采样点 |

四组各路径有序解码内容一致。表中候选与同输入单段比较；源 FLV 到单段本身的音频偏移范围约 1.14 ms，未将容器时间基差异称为零误差。
有限输入轮之后重新核对本机原生 DLL，SHA-256 仍为 `888A15993862950FEED714990E459F653B68BBC2CE30DA7D4BDA8DB9A5212C06`。

下一步是接入时钟生产/收尾、严格缺失或损坏处理、旧录制兼容与 UI 字节跟踪，再以实际生产路径复测，并补齐 HEVC、多轨、参数变更、手动停止和崩溃恢复等相邻场景。
候选参数及清单仍仅在探针内生效；本检查点不宣称生产缺陷已修复或双端验收完成。
没有手机操作、Root/LSP 变更、APK 构建或 3.2.0 发布。
