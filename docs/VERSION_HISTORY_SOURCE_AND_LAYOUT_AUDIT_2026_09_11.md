# 版本历史数据源与布局审计（2026-09-11）

## 范围与基线

- 基线：`e871354271b94a970f874a519273a3d7d990a904`。
- 对应验收组：A1-05 的关于/更新/历史版本入口，以及 A2-01 的窄屏、大字号和桌面双栏可达性。
- 修改前仓库审计：`local-artifacts/repository-audits/20260911T184003536Z-focused.json`。
- 修改前有效红测：`local-artifacts/build-records/20260911T104137864Z-quality-focused.json`，三项均失败；其中一项直接证明历史页仍写死上游仓库，另外两项稳定复现外部 JSON 数字类型和畸形嵌套字段造成的运行时类型异常。

本批只读审查维护仓库源码和随包 `assets/releases.json`，没有同步上游代码，没有调用真实更新网络、下载安装器或操作手机。

## 修改前问题

1. 版本检查和下载页已经由 `VersionUtil` 指向维护仓库 `wzgrx/pure_live`，版本历史页却另外构造 `liuchuancong/pure_live`，同一应用的当前版本和历史记录来自两个仓库。
2. `ReleaseModel.fromJson` 直接把外部值赋给 `String` / `int`，作者对象或文件数组中的单个畸形项会中断整批历史解析；数字字符串下载量、数字标题等常见兼容输入同样触发类型异常。
3. 刷新已有历史时先用全屏加载态替换列表；刷新失败后虽保留内存数据，却没有可见反馈。桌面当前选项还会在 `build` 内修改响应式状态。
4. 桌面双栏只按窗口宽度判断，宽窗口配大字号仍强制保留 320 像素侧栏；移动卡片、详情头和文件动作没有窄屏/大字号断点。
5. 移动详情使用固定 520 像素上限，并把头部、内容和关闭动作拆成不可共同滚动的纵向区域；3.0 倍文字下会溢出并隐藏动作。发布页、复制和下载图标缺少一致的标签及链接协议校验。

## 修订

- 历史默认加载器统一复用 `VersionUtil.mirror` 和“仅 GitHub 源”设置，当前版本、发布资产与历史记录保持同一维护仓库。
- 增加可独立验证的历史 payload 解析和 HTTP(S) 链接合同；忽略无版本号/非对象行，按日期和版本稳定排序，随包历史全部解析。
- `ReleaseModel`、作者和文件字段统一做字符串/整数归一化；畸形作者及文件项局部隔离，下载次数无效或为负时归零。
- 初次加载仍显示加载态；已有数据刷新时列表持续可见并显示顶部进度，刷新按钮暂时停用，失败后以本地化 Snackbar 告知且保留缓存。
- 刷新前记录所选版本，列表重排后按版本恢复选择；状态只在加载事务完成时提交，不在绘制期间写入。
- 大字号窗口回退为单栏；移动卡片、详情头和文件卡片按宽度/文字缩放改为堆叠布局。详情页整体滚动，发布、复制、下载、关闭均可滚动到达。
- 外部发布页只接受绝对 HTTP(S) 地址；发布、复制和下载动作具备文字或 Tooltip。加载器、外部浏览器和下载回调可注入，生产默认行为保持原入口。

## 验证

最终统一质量记录：`local-artifacts/build-records/20260911T105836512Z-quality-focused.json`；随附仓库审计：
`local-artifacts/repository-audits/20260911T105727865Z-focused.json`。

- 新增合同/模型测试 **6/6 PASS**：维护源一致、外部字段归一化、畸形嵌套隔离、过滤排序、随包历史完整解析及链接协议。
- 新增页面测试 **4/4 PASS**：`320×480` / 英文 / 3.0 倍文字下完整详情与四类动作、1000 像素桌面双栏切换、失败刷新保留、成功刷新重排后保留选择。
- 联合更新源、发布资产和翻译合同共 5 个测试文件，最终 **18/18 PASS**。
- 全库 `flutter analyze` 执行一次，结果 **No issues found**；质量任务结束时活跃重型进程为 **0**，实际 ADB 命令为 **0**。
- 仓库审计错误 **0**（已知告警 2）；四个修改/新增 Dart 文件格式检查无差异。

## 当前边界

A1-05、A2-01 保持 `RUN`。本批完成维护仓库历史源、解析容错、刷新事务、移动/桌面布局和离线动作合同；真实 GitHub/镜像失败切换、系统浏览器、系统剪贴板、Android/Windows 下载器、安装取消及最终候选页面仍需分别验证。宏观状态保持 **20 PASS / 33 RUN / 9 NR，共 42 组未闭环**。
