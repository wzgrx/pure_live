# Pure Live Android / Windows 全面实机测试计划（2026-08-28）

## 1. 目的与范围

本计划用于当前 `master` 的 Android 手机与 Windows x64 客户端实测、故障定位、修复回归和资源趋势核验。测试顺序固定为：已连接 Android 时先完成 Android；Android 阶段结束后再串行进行 Windows。重型测试、构建和打包遵守 `BUILD_POLICY.md`，Android 与 Windows 不并发构建。

本轮覆盖四层证据：

1. **静态合同**：页面入口、状态机、异步代次、订阅/Timer/播放器/录制资源释放、设置迁移和平台数据契约。
2. **确定性自动化**：受影响单元测试、Widget 测试、接口 fixture、设置持久化和生命周期回归。
3. **真实客户端操作**：Android ADB 实机和 Windows 本机窗口中的全部可操作页面、按钮、手势、模式切换与错误提示。
4. **运行趋势**：启动耗时、首页稳定、帧体验、CPU、内存、线程/句柄、网络恢复、长时间播放/弹幕/录制。

“通过”只代表对应案例和证据层成立。外部平台接口随时间变化；未登录平台、当前没有可用直播样本或缺少对应系统设备时，在报告中保留具体证据缺口。

## 2. 被测基线与证据目录

每次测试开始冻结以下信息：

- Git 完整 SHA、分支、工作树状态、上游/维护分支相对关系；
- App 语义版本、build number、包名、ABI、Flutter/Gradle/Windows 工具链版本；
- Android：设备序列号、型号、Android 版本、分辨率、密度、支持刷新率、当前刷新率、电量、温度、包版本；
- Windows：系统版本、显示器分辨率/缩放/最高刷新率、GPU、播放器内核、数据目录；
- 设置快照：主题、刷新率、导航、播放器、画中画、弹幕、本地互动、录制和平台登录状态。改变设置的案例结束后恢复快照；不修改真实账号 Cookie、关注关系和云端数据。

证据统一写入：

```text
local-artifacts/diagnostics/full-regression-20260828/
  android/{baseline,home,search,settings,playback,pip,record,fault,perf}/
  windows/{baseline,home,search,settings,playback,pip,record,fault,perf}/
  reports/
```

每个案例至少记录 `case-id`、开始/结束时间、步骤、期望、实际、截图/日志路径、资源采样和结论。日志先过滤 Cookie、令牌、用户输入和私有直播地址。

## 3. 严重程度与放行标准

| 级别 | 定义 | 处理 |
| --- | --- | --- |
| P0 | 启动失败、普遍黑屏、数据破坏、安装/升级破坏、播放器或录制主链路整体失效 | 立即停止后续发布链，定位首个错误状态 |
| P1 | 稳定复现的卡死、暂停、弹幕停更、模式切换损坏、主平台接口失效、资源持续无界增长 | 修复并重跑受影响矩阵及相邻模式 |
| P2 | 单页面功能、排序、布局、交互、文案或特定平台兼容错误 | 记录根因后定向修复与回归 |
| P3 | 低频视觉细节、证据不足或外部数据偶发现象 | 记录样本和后续条件 |

阶段放行条件：

- 启动、返回、首页导航、直播播放、弹幕、画质/线路、前后台、横竖屏、小窗、录制没有 P0/P1；
- 页面横向列表有边界，纵向列表能跟手滚动，刷新状态不会造成卡片乱跳或重复请求风暴；
- 无用户操作时不调用暂停/停止；模式切换后的播放器、弹幕会话和录制状态只保留一个有效代次；
- 退出直播/关闭小窗/停止录制后，Timer、Stream、WebSocket、纹理、FFmpeg 会话和页面 Controller 被取消或复用到明确所有者；
- 同一平台不同房间之间不串弹幕、不重复死循环，不接纳上一会话的迟到事件；
- CPU/内存以基线和趋势判断，不以单个瞬时值代替结论；长测结束后资源能够回落或进入稳定平台。

## 4. 执行阶段

### A0 — 静态盘点与保护

- [ ] A0-01 冻结 SHA、版本、设备和设置快照。
- [ ] A0-02 盘点路由与页面入口；确认没有死路由、重复 Controller、未释放 Worker/Timer。
- [ ] A0-03 盘点播放器、弹幕、画中画、录制生命周期所有权以及 app pause/resume 顺序。
- [ ] A0-04 盘点平台适配器字段类型、分页/排序、热度/在线人数、画质/线路、录制流地址合同。
- [ ] A0-05 检查重型进程互斥。静态检查与 ADB 轻量采样可先进行；Flutter/Gradle/Java 构建待资源守卫放行。

### A1 — Android 安装、升级与冷启动

- [ ] AND-BASE-01 显式选择一个 ADB transport；当同一手机出现两个无线序列时，所有命令固定传 `-s SERIAL`，避免装到一个 transport、采样另一个 transport。
- [ ] AND-BASE-02 核对已装 APK 包名、版本、ABI、签名类型和数据目录；覆盖安装前后关注、历史、标签、主题和核心设置保持。
- [ ] AND-BASE-03 强制停止后冷启动 5 次：没有白屏/黑屏/闪退/重复页面；启动期间首页数据开始刷新且不会阻塞首帧。
- [ ] AND-BASE-04 热启动 5 次、后台 5 秒/20 秒/2 分钟恢复；检查旧直播状态刷新、页面位置和播放器行为。
- [ ] AND-BASE-05 系统侧滑返回、应用返回键、Home 键和任务切换；首页返回进入后台，二级页逐层返回，直播页按产品规则退出。
- [ ] AND-BASE-06 采集启动 logcat、ANR/crash、首帧时间、进程 CPU/PSS、线程数、电量和温度。

### A2 — Android 首页、卡片、导航与刷新

- [ ] AND-HOME-01 底部“关注/热门/分区/录制中心”逐项点击；选中态、页面内容、返回状态一致。
- [ ] AND-HOME-02 底部导航快速切换 20 次；页面不重建风暴、不闪白、不丢列表位置、不出现重复请求。
- [ ] AND-HOME-03 关注页“正在直播/录制中/未开播”状态 Tab，及“全部/各平台”横向 Tab：拖动有边界，快速甩动后可反向操作，不会滑死。
- [ ] AND-HOME-04 关注页下拉刷新：触发距离合理、有刷新动画、一次手势只产生一次代次；刷新完成后旧卡片整批稳定替换，不按响应先后乱跳。
- [ ] AND-HOME-05 双击关注底部图标刷新；与下拉刷新、启动刷新、恢复刷新互斥或合并，不形成并发风暴。
- [ ] AND-HOME-06 启动时已下播房间从“在播”正确转为“未播”；进入房间后二次确认状态与首页最终状态一致。
- [ ] AND-HOME-07 封面加载：首屏占位尺寸固定，成功时淡入不改变卡片几何；失败、重试、手动刷新缩略图均保持布局稳定。
- [ ] AND-HOME-08 热门页平台 Tab 左右滑动边界、页面缓存、排序稳定；返回同平台不重复全量刷新。
- [ ] AND-HOME-09 分区页平台 Tab、分区网格、收藏分区、进入分区房间、返回位置和空态。
- [ ] AND-HOME-10 录制中心状态 Tab 横向边界、任务列表纵向边界、空态、打开目录和设置入口。
- [ ] AND-HOME-11 左上菜单：设置、关于、历史；右上更多：搜索、链接跳转、多画面同看。弹窗点击外部关闭、返回关闭、焦点顺序正常。
- [ ] AND-HOME-12 卡片短按进入、返回恢复；长按/快捷操作（若页面提供）不与纵向滚动冲突。
- [ ] AND-HOME-13 120Hz 设备上记录平台 Tab、卡片列表、二级页滚动帧时间；没有长时间 UI/GPU jank 连续峰值。

### A3 — Android 搜索、历史、工具箱与多画面

- [ ] AND-SEARCH-01 空关键词、中文、英文、房间号、主播名、前后空格、超长文本；搜索按钮和键盘提交一致。
- [ ] AND-SEARCH-02 “全部/各平台”横向筛选有首尾边界；选中态与结果平台一致，切换不会保留上一平台迟到结果。
- [ ] AND-SEARCH-03 智能/平台/观众/粉丝排序；直播优先、平台权重、数值缺失回退稳定且相等元素顺序确定。
- [ ] AND-SEARCH-04 包含未开播开关、加载更多、全部加载完、错误重试、继续网页搜索和系统浏览器回退。
- [ ] AND-SEARCH-05 连续输入、快速切平台、取消页面：旧请求结果不会覆盖新查询；分页不会重复房间。
- [ ] AND-HISTORY-01 历史双列/自适应列数、日期显示、滚动边界；数量预设、自定义、无限、清空确认和取消。
- [ ] AND-TOOL-01 房间链接跳转：合法支持链接、非法链接、空输入、清空、错误提示；进入房间后返回正常。
- [ ] AND-TOOL-02 解析直链：支持平台、无效链接、超时和复制结果；敏感参数不进入测试报告。
- [ ] AND-MULTI-01 1×1、1×2、2×2、1+3 布局；添加/换房/关闭格子，焦点音频唯一。
- [ ] AND-MULTI-02 单格播放暂停、刷新、弹幕总开关/设置、画质、线路、音量；沉浸/全屏进入退出及系统返回。

### A4 — Android 设置全量 UI

通用要求：每个开关、单选、滑块、输入框和排序控件都测试“修改后即时 UI”“退出重进”“杀进程重启”“恢复原值”；滑块拖动过程中不触发高频持久化或整页重建。

- [ ] AND-SET-01 主题：亮/暗/跟随系统、主题色、动态颜色、加载动画样式/颜色/恢复默认；设置页和直播弹幕设置的主题同步。
- [ ] AND-SET-02 网格横纵间距、分页控件（每页数量、跳页、回顶部）、语言、字体下载/应用/删除/字重、正文与标题字号、全局文本缩放。
- [ ] AND-SET-03 刷新：关注自动刷新、后台恢复刷新、间隔、最大并发、封面自动刷新及间隔。验证禁用后没有后台 Timer，启用后只有一个调度器。
- [ ] AND-SET-04 视频音频：全局静音、移动/桌面默认音量、Wi-Fi/移动网络首选画质。
- [ ] AND-SET-05 后台播放、ASMR 开关和自定义助眠时长；到时停止、取消、重设、前后台和进程重启恢复逻辑。
- [ ] AND-SET-06 退出画中画、默认全屏、屏幕常亮；Windows 专属项在 Android 不误显示。
- [ ] AND-SET-07 竖屏直播：智能检测、自适应高度、平衡/沉浸/兼容布局、全屏策略、小窗跟随源比例、弹幕模式、记住房间覆盖、诊断开关、重置。
- [ ] AND-SET-08 热度/真实在线人数总模式；Bilibili、斗鱼、虎牙、抖音、快手、YY、CC、Twitch、SOOP 的能力说明、支持开关、缺失值回退与排行合同。
- [ ] AND-SET-09 弹幕全局开关、字体、屏蔽管理；画中画弹幕开关、去表情、自动缩放、原色、颜色、字号、字重、速度、透明度、区域、最大可见、间隔、动态/固定 FPS、恢复默认；预览实时变化且滚动页面时保持可见策略合理。
- [ ] AND-SET-10 播放器内核切换、硬解、兼容模式、强制销毁、代理；移动端隐藏 Windows RTX/VSR 或 MPV 专属无效项。
- [ ] AND-SET-11 应用代理与播放器代理分别启停、地址/端口校验、空值、非法端口、保存和恢复。
- [ ] AND-SET-12 本地互动：总开关、平台资源包、昵称、头衔、覆盖弹幕、平台/等级徽章、礼物特效、金币充值、历史清理、弹幕样式入口。
- [ ] AND-SET-13 通用：省电/均衡/最高刷新率模式即时生效，显示设备最高 Hz；倒计时关闭开关、预设、自定义、剩余时间；更新检查和启动动画。
- [ ] AND-SET-14 导航：多画面开关、底部菜单显示、至少保留一项、拖动排序、重启保持。
- [ ] AND-SET-15 平台：显示/隐藏和拖动排序、首选平台、第三方登录入口、标签管理的增删改置顶排序。
- [ ] AND-SET-16 缓存：刷新缓存大小、刷新缩略图、清理确认/取消；清理后首页能重新加载。
- [ ] AND-SET-17 IPTV：列表源、网络/本地导入、EPG、同步开关/间隔、UA、选择源、错误输入和取消。
- [ ] AND-SET-18 备份：本地目录、创建/恢复、配置预览、WebDAV、TV 同步入口、日志开关/浏览器/目录。只使用隔离测试文件，不覆盖用户现有备份。
- [ ] AND-SET-19 第三方认证入口与状态只做页面/格式/返回核验；不退出用户现有账号，不写入虚构 Cookie。
- [ ] AND-SET-20 关于、版本检查、版本历史、许可证、项目链接；离线时错误提示不阻塞页面。

### A5 — Android 播放、弹幕、方向与小窗核心矩阵

每个样本先记录平台、房间 ID、实际源尺寸/比例、播放器上报尺寸、识别结果、画质/线路数量和弹幕连接状态。至少准备：普通 16:9、真实 9:16、非标准比例、临时无尺寸/尺寸迟到各一种 fixture；真实平台样本按当时可用房间补充。

- [ ] AND-PLAY-01 首次打开：加载 UI、首帧、控件、返回、关注/录制按钮、热度或在线人数、当前画质/线路。
- [ ] AND-PLAY-02 普通 16:9：竖屏页面、横屏全屏、小窗均保持比例，不被竖屏检测误判。
- [ ] AND-PLAY-03 真实 9:16：竖屏充分利用可用高度且不拉伸；横屏居中并按设置显示两侧背景；小窗跟随纵向比例和系统限制。
- [ ] AND-PLAY-04 尺寸迟到、先报错误尺寸、线路切换后尺寸变化、重启应用后再进：只有当前播放代次可提交几何，稳定结果不被迟到事件覆盖。
- [ ] AND-PLAY-05 全屏按钮、系统旋转、方向锁、系统返回；竖屏源也有清晰的全屏进入/退出路径。
- [ ] AND-PLAY-06 竖屏页面顶部图标、横屏右上图标顺序保持“耳机/投屏/小窗”；按钮触控区域不重叠。
- [ ] AND-PLAY-07 音频→视频连续切换 10 次：不无限转圈、不自动暂停、不白屏；视频恢复期间有明确过渡 UI，旧 open/pause 事件不污染新代次。
- [ ] AND-PLAY-08 前台→后台→前台、锁屏→解锁；根据后台播放设置保持或恢复，画面/弹幕/控件同步。
- [ ] AND-PLAY-09 应用小窗进入→恢复竖屏重复 10 次；恢复后弹幕列表继续增长，列表订阅不挂起、不重复。
- [ ] AND-PLAY-10 系统画中画进入/恢复/关闭重复 10 次；比例、控制按钮、弹幕、音频焦点和页面返回正确。
- [ ] AND-PLAY-11 清晰度逐项切换：当前选中、请求的 quality id、实际流 URL/码率/分辨率变化一致；失败时回退并提示，不保留错误选中态。
- [ ] AND-PLAY-12 线路逐项切换：当前 line/cursor、实际 URL 主机或标识变化；画质和线路组合保持，不被旧请求覆盖。
- [ ] AND-PLAY-13 清晰度与线路面板按内容自适应，选项占主要视觉；横屏右半屏紧凑布局、内容可完整滚动且边界正常。
- [ ] AND-PLAY-14 播放/暂停、刷新、音量、静音、定时停止、投屏入口、直播记录双列面板和小窗快捷按钮。
- [ ] AND-PLAY-15 直播记录：状态/录播/观看记录 Tab、日期、数量设置、双列卡片、进入/返回和滚动边界。
- [ ] AND-DAN-01 弹幕连接、断开、重连、心跳；房间 A→B→A 不出现错房间、旧消息、重复消息或死循环。
- [ ] AND-DAN-02 竖屏覆盖弹幕、横屏覆盖弹幕、小窗弹幕、下方弹幕列表共享同一有效会话但各自展示策略独立。
- [ ] AND-DAN-03 弹幕设置模板、保存/恢复、区域、上下边距、透明度、速度、字号、字重、描边、动态/固定 FPS 即时预览和即时生效。
- [ ] AND-DAN-04 快速弹幕滚动平滑；同一消息不同视图速度策略一致，不因页面 rebuild 重启动画。
- [ ] AND-DAN-05 弹幕列表手指上滑一次即进入阅读暂停；继续滚动跟手；回到底部或点击恢复后只追加一次新消息。
- [ ] AND-DAN-06 消息长按：复制、屏蔽用户、屏蔽关键词；双击复制；屏蔽管理增删、相似度过滤、重复折叠。
- [ ] AND-DAN-07 画面点按/长按弹幕与播放器单击、双击、拖动手势无冲突；关闭交互开关后不拦截播放器手势。
- [ ] AND-DAN-08 本地消息发送后进入 2–3 秒交付队列，在覆盖层和列表同步出现；昵称不被错误替换为 `xxx`，重进房间不重复投递。
- [ ] AND-DAN-09 横屏本地输入和样式编辑：左侧实时预览、右侧设置；预设、位置、颜色、渐变、字号、粗体、斜体、描边、阴影同步到竖屏和设置页。

### A6 — Android 平台适配器矩阵

对 Bilibili、斗鱼、虎牙、抖音、快手、YY、CC、Twitch、SOOP、IPTV 分平台记录以下合同；“公开接口未提供”与“请求失败”分开显示：

- [ ] 首页热门/分区/搜索可用性，分页、去重和错误类型；
- [ ] 房间详情、开播状态、标题、主播、封面、关注状态；
- [ ] 热度与真实在线人数的来源、单位、可用性、显示与降序排序；
- [ ] 画质原始标签、标准化中文标签、quality id、默认/最高选择准确性；
- [ ] 线路数量、cursor/id、请求头/Cookie、最终播放地址；
- [ ] 播放地址有效期、重定向、协议、解码能力与刷新策略；
- [ ] 弹幕匿名/登录要求、WebSocket 地址、心跳、重连和房间隔离；
- [ ] 录制地址是否与播放地址合同不同，FFmpeg 需要的 headers、协议白名单、时间戳和容器兼容。

每个平台至少完成一个当前在播样本的详情/播放/画质/线路/弹幕/录制探测；缺少直播样本时保留 fixture 自动化与探测时间，不借用另一平台结论。

### A7 — Android 录制、后台与本地互动

- [ ] AND-REC-01 从直播页开始录制，录制中心立即出现正确房间、平台、画质、状态、时间、实时大小、速度和码率。
- [ ] AND-REC-02 停止、取消等待、失败重试、立即检查、重连；状态只按允许路径转换，不全部显示“等待开播”。
- [ ] AND-REC-03 Bilibili/斗鱼/虎牙/抖音/快手/YY/CC/Twitch/SOOP/IPTV 的流地址和 headers 分别核验；有可用样本才执行短录制。
- [ ] AND-REC-04 录制文件可由 ffprobe 读取，持续时间接近墙钟，文件大小增长；0B、超大时长、负速度和错误阶段不出现。
- [ ] AND-REC-05 录制设置：目录、缓存上限、默认画质、拼音目录、最佳流、读写超时、队列、分段、最大任务、重连、轮询/退避和开机启动。
- [ ] AND-REC-06 录制中心横/纵滚动边界、卡片自适应列数、长错误文本、任务删除确认、重启后持久化恢复。
- [ ] AND-BG-01 后台播放和 ASMR 分开建模；关闭 ASMR 后普通直播不显示音频状态，耳机/投屏/小窗各自语义唯一。
- [ ] AND-BG-02 前台服务通知、音频焦点、系统省电提示、网络切换后恢复；不承诺绕过系统厂商的强制终止，但应用内部状态可恢复。
- [ ] AND-LOCAL-01 各平台本地资源包、头衔/等级/礼物效果、金币扣减、历史和设置持久化；明确仅在本地展示。

### A8 — Android 故障注入、性能与稳定性

- [ ] AND-FAULT-01 飞行模式/断网 10–30 秒后恢复；首页请求、直播、弹幕、录制采用有限退避且页面可手动恢复。
- [ ] AND-FAULT-02 Wi-Fi↔蜂窝网络语义 fixture、DNS 错误、HTTP 超时、429、5xx、空响应、字段类型漂移；错误分类明确。
- [ ] AND-FAULT-03 流地址过期、CDN 重定向、解码错误、线路失效；只在明确错误时恢复，不以固定延时自动暂停。
- [ ] AND-FAULT-04 快速进入/退出房间 20 次、A/B 房间切换 20 次、横竖屏/小窗循环 20 次；无崩溃、ANR 和旧会话回流。
- [ ] AND-PERF-01 首页静置 10 分钟、列表滑动 5 分钟、播放+弹幕 30 分钟、画中画 15 分钟、录制 15 分钟的 CPU/PSS/线程/温度趋势。
- [ ] AND-PERF-02 页面退出后 60 秒资源回落；重复 10 次后增量不持续线性上升。图片区缓存、弹幕队列和历史列表有明确上限。
- [ ] AND-PERF-03 60/90/120Hz 模式实时生效；省电/均衡/最高档的帧策略和小窗/覆盖弹幕 FPS 合同一致，耗电提示准确。
- [ ] AND-PERF-04 采集 `gfxinfo framestats`、CPU、meminfo、线程和热状态；以 P50/P90/P95/P99 和 jank 数量比较基线与修复后。

## 5. Windows x64 全面客户端矩阵

Windows 阶段复用 A2–A8 的公共业务案例，并补充桌面特有检查：

### W1 — 安装、便携数据与升级

- [ ] WIN-BASE-01 Debug/Release 产物来源 SHA、版本、文件清单和依赖 DLL；启动 5 次和多实例入口。
- [ ] WIN-BASE-02 安装目录可选择非系统盘；便携数据策略、用户选择目录、旧版本配置/关注/历史迁移，升级不丢数据。
- [ ] WIN-BASE-03 卸载范围只覆盖本版本管理的文件；保留/清理用户数据的选择和说明准确。
- [ ] WIN-BASE-04 首次启动、热启动、异常退出后恢复；没有旧生成物 DLL 污染或重复插件注册。

### W2 — 窗口、输入与高刷新率

- [ ] WIN-UI-01 普通窗口最大化/还原/自定义尺寸、缩放 100/125/150/200%、多显示器拖动。
- [ ] WIN-UI-02 鼠标滚轮、触控板、拖动滚动条、横向平台 Tab；滚轮增量连续，不出现一次一格的卡顿。
- [ ] WIN-UI-03 键盘 Tab/Enter/Escape/方向键；Escape 按层退出弹窗、全屏、直播页，不直接终止应用。
- [ ] WIN-UI-04 动态刷新率信息和显示器切换；60/120/144/165/240Hz 可用模式按实际显示器刷新。
- [ ] WIN-UI-05 小窗进入/恢复/关闭；“始终置顶”关闭后切换其他应用不遮挡，开启时语义明确；位置记忆和重置。
- [ ] WIN-UI-06 全屏、音频、画中画、播放器控件使用真实鼠标坐标确认状态变化，不能仅凭 tooltip 判定成功。

### W3 — Windows 公共功能与播放器

- [ ] WIN-COMMON-01 首页、搜索、分区、历史、设置、备份、IPTV、多画面、录制中心按 Android 公共矩阵逐项执行。
- [ ] WIN-PLAY-01 MediaKit/MPV 可用内核、硬解、GPU/音频输出、兼容模式；切内核前后播放/弹幕/录制互不污染。
- [ ] WIN-PLAY-02 普通/全屏/小窗/多窗口的画面比例、画质线路、直播记录、本地弹幕和鼠标手势。
- [ ] WIN-PLAY-03 关闭直播后播放器纹理、解码线程和网络连接释放；新窗口关闭不影响主窗口状态。
- [ ] WIN-REC-01 录制开始、实时大小/时长/码率、停止、文件 ffprobe、失败重连和目录打开。

### W4 — Windows 性能、故障和长测

- [ ] WIN-PERF-01 首页静置 15 分钟、列表连续滚动 5 分钟、播放+弹幕 30 分钟、录制 30 分钟、小窗 15 分钟。
- [ ] WIN-PERF-02 每 5 秒记录 working set/private bytes/handles/threads/GPU/CPU；退出直播后观察 60 秒回落。使用 `tool/sample_windows_runtime.ps1 -IncludeGpu` 同时采集进程 GPU engine sum、3D、Video Decode、Video Processing、Copy、独显/共享显存，以及显卡驱动、当前分辨率和刷新率；计数器不可用时记录 `null`，不把缺失数据伪装成 0。GPU engine sum 是多个引擎/适配器实例之和，允许超过 100%，判读时必须同时查看分项。
- [ ] WIN-PERF-03 A/B 房间切换 20 次、全屏/小窗 20 次、打开关闭二级页 50 次；资源不线性增长。
- [ ] WIN-FAULT-01 断网、代理错误、DNS/超时、流断开、窗口失焦、显示器休眠恢复；状态可解释且可恢复。
- [ ] WIN-SOAK-01 1–2 小时综合长测：播放、弹幕、定时切模式、短录制、网络抖动；若趋势提前稳定可按计划提前结束。

## 6. 修复与回归规则

1. 复现后先写 `provenance`：`upstream-existing`、`fork-regression`、`integration-conflict`、`external-drift`、`environment-or-data` 或 `not-reproduced`。
2. 找到第一个错误状态和所有者；不以增加任意延时、无限刷新、全局重建或无限重试掩盖问题。
3. 先补确定性回归，再做最小修复；播放器来源、自动恢复、几何、录制分开提交并可独立回滚。
4. 修改完成后只跑受影响测试；代码稳定后执行一次 Analyze。重型命令通过资源守卫串行执行。
5. Android 修复后重新安装来自当前 SHA 的 APK，重跑原案例、相邻模式和资源回落；随后才进入 Windows。
6. 每个结果标记 `pass`、`fail`、`blocked-by-sample`、`external-drift` 或 `not-run`，并附证据路径。

## 7. 最终报告结构

- 构建/安装基线和 SHA；
- Android 逐阶段通过/失败/样本缺口数量；
- Windows 逐阶段通过/失败数量；
- 新发现问题：来源、根因、修复提交、回归证据；
- 平台接口能力矩阵和探测时间；
- CPU、内存、线程、句柄、帧时间和温度对比；
- 未覆盖平台/登录态/直播样本和剩余风险；
- 本轮实际产物路径、哈希，以及是否进入正式发布阶段。
