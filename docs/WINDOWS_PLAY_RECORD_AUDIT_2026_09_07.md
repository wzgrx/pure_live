# Windows 虎牙播放、选择器与录制原生补证（2026-09-07）

复用 [fadd5bdb Windows Debug 候选](WINDOWS_AUXILIARY_NATIVE_AUDIT_2026_09_07.md)，版本 3.1.8+4121，独立 `acceptance_fadd5bdb` 数据目录。本轮没有应用代码修改、构建、完整重测或发布。上一轮为有效进展，本轮继续真实运行缺口。

ADB 只读复核 `25102RKBEC / myron`，明确指定 `-s 192.168.1.2:5555`；手机前台仍为 Bilibili，前两轮争用尚未协调，因此未抢占或输入，继续 Windows。没有 MT、Root 或 LSP 改动。

## 实际播放与操作

- 独立 PID **77992**，候选可执行文件路径/来源见本地 `process.json`。通过 Computer Use 操作窗口，截图 1276×718，不用命令模拟 UI。
- Bilibili 热门加载后切换虎牙，打开杨齐家丶的 `【二台军师】3X3`。实际视频首帧和远端弹幕出现，后续截图中的直播内容与弹幕持续变化；未做听音验收。
- 画质 **蓝光20M → 蓝光8M**，线路 **1 → 2**。UI 提交后保持 8M/线路2；`player-transitions.log` 记录 session 1/2/3，以及主机从 `al.flv.huya.com` 变成 `tx.flv.huya.com`，不是只打开菜单。
- 录制期间经播放器菜单“获取直链”进入真实清晰度对话框，选择独立的 4M 档位后展示六条 FLV/HLS 线路；点击取消，返回原播放器且仍是 **8M/线路2**，录制继续。没有点击复制，因此不宣称剪贴板写入已验。
- 录制中心显示 **20M/线路1**，与播放器档位不同。源码 `recorder_controller.dart` 使用 `settings.defaultQuality` 独立解析并回写实际选中档位/线路；这不是本次取直链动作修改播放器或录制设置的证据。
- 手动停止后，录制中心经历“处理中 → 已停止”；返回播放器后重新出现视频与弹幕，再退出直播间回到热门列表。立即录制留下“已监控”任务的既有产品语义仍保留，未删除隔离数据。

## 最终文件与大小修复闭环

文件保留于独立候选的 `AppData/acceptance_fadd5bdb/RECORDS/huya/杨齐家丶/2026-09-07/11-10-11/20260907_111011_171.mp4`。

| 指标 | 本轮实测 |
| --- | --- |
| 最终字节数 | **33,832,167** |
| SHA-256 | `DB9209733312E40A8549F288AACFA74EE8EB43EFC09BA707E9755418C464643C` |
| 容器时长 | **207.609667 秒** |
| 视频 | H.264，1920×1080，60 fps，12,455 帧，207.582667 秒 |
| 音频 | AAC，8,939 帧，207.563107 秒 |
| 卡片停止前/最终 | 处理中 **34.82 MB** → 已停止 **32.26 MB**，最终时长 `00:03:27` |
| 对账 | `33,832,167 / 1024² = 32.264868…`，保留两位小数 **32.26**，与卡片一致 |
| 完整解码 | FFmpeg 退出 0，错误日志 **0 B**；非仅探测文件头 |
| DTS 逐包 | 视频/音频均 0 个非正间隔、0 个 >2秒间隔，最大间隔分别 0.018 / 0.023243 秒 |

**W3-05 中“最终 MP4 替换临时 TS 字节”的新包原生复验子项已完成。** 本样本没有断流或两次签名续接，不扩大为全部录制验收。开始/停止点击的记录差约 201.950 秒，而媒体约 207.610 秒，差约 5.660 秒；尚未验证源端预缓冲/起止记录开销的具体贡献。逐包无大跳变并不证明媒体时长与墙钟完全相等。

## 资源与退出

使用既有只读采样器，采样对象均为 PID 77992；GPU 未采样，不填 0 代替未知。

| 场景 | 响应 | CPU均值/P95（24逻辑核归一） | Working Set 首→末 | Private Bytes 首→末 | 句柄/线程 首→末 |
| --- | --- | --- | --- | --- | --- |
| 录制、选择器及录制中心混合交互，180.614秒 | 19/19 | 2.3805% / 3.307% | 895.2109→935.4688 MiB | 1199.5703→1217.1875 MiB，峰值1450.2148 | 2120→2106 / 337→332 |
| 退出直播间回热门列表，60.612秒 | 13/13 | 2.3266% / 2.605% | 819.5430→725.5977 MiB | 1086.5352→955.8750 MiB | 1709→1324 / 245→156 |

第二段资源明确回落，日志有 VideoOutput/Texture 释放；但列表 CPU 仍约 2.3%，没有归因为 Debug 开销或判定性能通过。第一段不是全程前台视频播放的纯场景，第二段开始阶段并行做了本地媒体解码；其 CPU 指标属于应用进程，仍应保留测试负载背景。后续需分离静止列表、滚动、视频和弹幕负载，并用 Release 同链定位。

通过窗口关闭 → 退出确认正常结束，随后查询 PID 77992 已消失；测试 FFmpeg 解码进程和两个采样会话均退出 0，未终止其他应用或后台工作。

## 证据与剩余

本地目录 `local-artifacts/windows-playback-fadd5bdb-20260907/` 包含 process、UI accessibility 摘录、原始 stdout/stderr、播放器转移摘录、两份采样 CSV/JSON、ffprobe、文件哈希、逐包和完整解码结果、开始/停止点击时间。视觉判断使用任务中的原生截图；accessibility 可能滞后于页面转场，不能把其空树解释为页面缺失。带临时签名的线路原始数据只留本地，不提交到文档。

W2-04 / W3-05 更新本候选增量但保持 RUN。仍需虎牙跨两次续接、其余平台短录/代表长录、复制/DLNA、窗口/音频/PiP组合和 Release 性能对照。历史总表仍 20 PASS / 32 RUN / 10 NR；42 大项未闭环，不以本轮单平台成功替代整个目标。
