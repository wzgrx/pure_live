# niconico 元数据、实际画质发现与播放适配（2026-09-10）

## 范围与实现

基线 `2b9be053f3afa7b16c24d12163e6732f4a51fb49`。本批为新平台分阶段开发，不合并上游，不注册未完成的目录或导航能力。

- `NiconicoQualityCatalog` 每次重新读取节目元数据，创建独立临时 seat，按实际 HLS master 的分辨率和码率生成公开选择项。同尺寸不同码率保持区分，重复选择器或歧义音轨报告结构错误。解析一次，复用 HLS 选流验证。
- 发现会话在返回前完成清理；取消、迟到创建、远端结束、主地址变化、Cookie 更新及清理失败均有确定性测试。默认 30 秒发现期限通过请求取消传递到生产网络实现，清理任务仍等待实际结束；不把期限声称为任意注入实现的硬终止保证。
- `NiconicoSite` 提供严格元数据刷新、实际画质、播放/恢复/录制游标的公开 owned recipe；公开状态仅含节目 ID、分辨率、码率，不保存会话 bootstrap、媒体 URL 或 Cookie。实际消费重新取得会话。
- 区分直播状态与访问条件；累计观看次数使用 totalViewers，不冒充当前在线人数。收藏绑定本次节目；弹幕尚未接入，提示已进入中英文词典。
- 实际用户节目响应含动态 screenshot 与静态 404 图标，因此封面优先读取 screenshot，缺失时使用 thumbnail；头像读取公开 supplier 图标。可选图片只接受校验后的 HTTPS nimg.jp/nicovideo.jp 子域地址，格式异常不丢弃其余节目元数据。

## 失败与修订

首轮 162 项为 160 PASS / 2 FAIL：一项测试误将既有 onlineViewers 空字符串默认值期望为 null；另一项发现 Uri 对显式默认 443 端口归一化，使图片端口约束未生效，补充原始 authority 校验。保留 `20260910T063619544Z-niconico-site-initial.json`。

第二轮 164/164 PASS，七文件严格分析有一项 prefer_initializing_formals 提示，记录 `20260910T064108187Z-niconico-site-regression.json`。随后整理构造函数时，文本替换遗漏旧 initializer，导致两个新测试文件编译失败；其余 125 项通过，记录 `20260910T064738296Z-niconico-site-final.json`。移除多余 initializer 后，仅重跑直接受影响的两个新测试文件并分析七文件，其余 125 项按未变输入复用。

构造函数修订后新增 39/39 PASS，严格分析提示移除初始化形参的冗余类型，记录 `20260910T065050209Z-niconico-site-constructor-fix.json`；继续移除该标注后复核，保留各次失败记录，不把多轮通过数累加。

## 最终验证

最终两个新测试文件与 opt-in 生产探针 **40/40 PASS**（39 项确定性测试 + 1 项真实节目），七 Dart 文件严格分析 **No issues found**。记录 `local-artifacts/build-records/20260910T065323980Z-niconico-site-final-production.json`，耗时 140.87 秒，终态活跃重型进程 0；构建资源互斥及监控按 finally 释放，保留增量缓存。此前 125 项相邻通过证据按未变输入复用，总确定性覆盖为 164 项，不累加各轮运行数。

生产探针于 2026-09-10 06:51:59 UTC 经本机 Clash 127.0.0.1:7897 读取节目 lv351358479，实际返回 1280×720 / 3280800 bps、854×480 / 1630800 bps、512×288 / 508800 bps；封面/头像均存在，累计观看语义正确。主列表 1115 B，SHA-256 `2245584c8b88d28d47cbefbc1658503fc6b11a3ff23d2f97fbf6109c1e4df6f7`；唯一发现 seat 关闭且清理成功，Cookie 保留数 0。报告 `local-artifacts/niconico-site-20260910/20260910T065102921Z-production.json`。逐档 owned 配方回执通过，未媒体解码或真实长录。

同日 06:34:02 UTC 的公开节目页 HTTP 200、53410 B 和原始响应哈希保存在本地 `http-evidence.json`；原始匿名 bootstrap 仅保留在忽略目录，不进入 Git。首页实际包含 seed 列表但目录分页合同尚未实现。本轮网络验证未登录账号，也未变更系统代理。无本地化上下文的 headless 测试提示缺少翻译键；静态核对中英文 JSON 五键均存在，不视为原生文案呈现已验收。

## 待完成与交付边界

本站尚未加入应用站点列表：目录/搜索/分享/导航、注册/能力声明/设置迁移仍待接入和验证。LiveSite 的画质接口没有外层取消参数，本批 catalog 的局部取消证据不等于页面立即取消发现的证据，注册前继续审查这一调用链。音频专用直播尚无覆盖。真实播放器 GUI、多路原生解码、长期录制、自然结束自动恢复以及 Android/Windows 全面验收继续。

保持 19 站点 + IPTV、8 组未注册；历史 62 组仍有 42 组未闭环。版本保持 3.1.8+4121；Android 候选仍 152cf151，Windows 候选仍 2fb471d3，均不包含本批。本轮未操作手机、安装 APK、构建或发布；不变更 ADB、Root、LSP 或 MT 配置。完整验收后再交付 3.2.0。

回滚：撤回本批 Site/catalog、可选图片字段、HLS 公共 select 包装、五项双语提示和对应测试/探针，保留前序会话输入及播放/录制消费者实现。
