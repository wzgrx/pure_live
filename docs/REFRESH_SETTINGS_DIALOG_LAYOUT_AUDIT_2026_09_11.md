# 刷新设置选择器布局与时间边界审计（2026-09-11）

## 范围与基线

- 基线：`18b511d654602c320073618a8d11e5d60d26faf3`。
- 对应验收组：A2-01 刷新设置二级页，以及刷新间隔、首页并发数和缩略图刷新间隔三个三级选择器。
- 固定矩阵：Windows `320×480`、英文、3.0 倍文字；同时启用自动刷新收藏和自动刷新缩略图，连续选择三个
  选择器的末项。

## 修改前复现与代码审查

修改前复现记录：`local-artifacts/build-records/20260911T072712211Z-quality-focused.json`。

旧选择器把标题、说明和一个按屏幕高度固定的内部列表放入受限 `Column`。第一个“Refresh Interval”弹窗在固定
矩阵中已经稳定向下溢出 **548 像素**；选项虽然属于可滚动列表，标题与内部列表仍无法共同适应可用高度，后续
并发数和缩略图间隔也沿用相同结构。

代码审查另确认刷新时间只依赖选择器产生正常值：本地持久值或备份可以把收藏刷新间隔写成 0/负数，页面仍显示
自动刷新已启用，但收藏计时器不会启动；异常大值则会形成近似永久不刷新的配置。缩略图调度虽然在消费端钳制，
页面和导出值仍会保留错误数值。

## 修订

- 三个选择器统一为整体可滚动的 `AlertDialog`；标题、并发说明和所有单选行共享同一滚动面，不再叠加固定高度
  子列表。
- 选项继续使用 `RadioListTile`，保持整行命中；固定矩阵下分别覆盖 12 个收藏间隔、20 个并发值和 8 个缩略图
  间隔，并连续选择三个末项。
- 刷新时间新增统一 `5..360` 分钟边界；启动读取、本版备份解析和旧版配置提取都在发布配置前归一化，避免开关
  与实际计时器状态相悖。
- 并发数原有 `1..20` 边界和默认推荐值 4 保持不变。

## 验证

最终统一质量记录：`local-artifacts/build-records/20260911T073344515Z-quality-focused.json`。

- 刷新配置、窄屏选择器、设置生命周期、备份预检/往返/迁移、缓存调度、收藏刷新生命周期/启动策略和历史并发
  等 10 个测试文件共 **87/87 PASS**。
- 全库 `flutter analyze` 执行一次，**No issues found**。
- 仓库完整性审计错误为 **0**（已知告警 2）；质量任务结束时重型进程为 **0**，实际 ADB 命令为 **0**。
- 本批未操作手机、发起真实平台刷新、构建候选、修改版本或发布 3.2.0。

## 当前边界

A2-01 保持 `RUN`。本批闭环 Windows 确定性刷新设置长页和三个选择器；Android 触控、真实平台请求并发/限流、
前后台恢复、覆盖升级后的持久化和长时缩略图刷新继续由当前最终候选执行。
