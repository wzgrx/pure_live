# 猫耳分类多状态图标审计（2026-09-08）

## 复现与来源

基线 `1263dcdc`，应用 `3.1.8+4121`；前批 Windows `f3de664a` 原生截图中，猫耳六个分类卡片、收藏卡片及关注按钮头像显示了两种状态各一部分。不是图片请求失败。

- `MissevanApi.categories → LiveArea.areaPic → AreaCard / FavoriteAreaFloatingButton` 把双帧竖排图片作为普通居中封面；方形视口加 `BoxFit.cover` 居中截取恰好跨过两帧分界。
- 猫耳接入来自维护分支 `7aaecb8e`，该资源合同遗漏归为 `fork-regression`。上游冻结提交 `c6c9bd70aedc503c003110dae10a83ad0bb891d8` 没有此适配器。
- 相邻 `AreaPicMapper` 仅按分类名称缓存通用封面，会把“音乐”等名称的有效封面替换成状态图标，还会读取已持久化的这类条目。这个通用缓存机制在上述冻结上游也存在；本次猫耳接入触发其资源类型缺口。不合并上游。

## 官方资源证据

本次从[公开元数据](https://fm.missevan.com/api/v2/meta/data)取得六个 tab，再核对[官方页面](https://fm.missevan.com/)引用的 JS/CSS 和 12 张原图。证据在本地忽略目录 `local-artifacts/missevan-category-art-20260908/`，包括 URL、尺寸、SHA256、逐帧不透明像素颜色统计；没有编辑原图。

| 资源 | 尺寸 | 上半帧 | 下半帧 |
| --- | --- | --- | --- |
| API `icon_url` | 60×120 | 灰色 | 各分类彩色 |
| API `web_icon_url` | 40×80 | 各分类彩色 | 白色 |

六类为配音、音乐、情感、新星、放松、古风。JS 使用 `web_icon_url`；CSS 设置 20×20 视口、背景宽 100%，普通态 Y=0、选中态 Y=-20。**两套资源的彩色帧位置不同**，不把官网的 -20 位移机械用于移动图标，也不把所有高图当作状态图。

## 本轮设备能力预检

用户补充 MT APK MCP 后，仅重新连接并读取明确 `192.168.1.2:5555` 的型号/代号，确认 `25102RKBEC / myron`，再读取 Root 与既有转发。`su -c id` 返回 uid=0；普通 shell `command -v lspctl` 没有路径输出，不推断安装状态。转发仍为本机 18787 到手机 8787。

HTTP `initialize → notifications/initialized → tools/list` 成功，协商协议 `2025-06-18`，服务 `MT APK MCP 0.1.0`，26 个工具，实际 inputSchema 已读取并保留在 `local-artifacts/mt-preflight-20260908/`。没有调用 APK 枚举、open、修改或构建工具，也没有安装、前台输入、唤醒、常亮设置或模块变更。此为只读连接预检，不经会唤醒设备的实测包装器，也不记为 Android 应用验收。

## 修订边界

- `category_artwork.dart` 仅识别已核验主机、六个精确文件名及其 web 版本，移动资源取下帧、web 资源取上帧。未知文件、其他主机/路径、附带用户信息或非标准端口继续沿用普通居中封面，不引入宽高猜测。
- 分类/收藏共用的 `AreaCard` 与 32px 关注头像使用同一对齐规则；保留网络请求头、内存尺寸上限、缓存管理器、占位/错误内容、点击和关注逻辑。
- 通用封面缓存读入时过滤历史状态图，新更新也跳过这些资源，不覆盖已有可复用封面。原始收藏 URL、身份键和备份字段保持；无需删除用户数据。

## 验证与交付状态

- 首次测试暴露两个夹具问题：空 `shortName` 与模型既有 null→空串规则不一致，以及测试默认 DPR 与图片缓存种子尺寸不一致。分别修订夹具为完整旧记录和显式 DPR=1，原失败日志单独保留，不计作产品缺陷。
- 修正夹具后，基线确定性红测为 **1 通过、3 失败**：两条新/旧缓存隔离失败，以及实际卡片上四分之一处应取绿色却读到红色；普通封面像素对照通过。记录 `20260907T193345839Z-area-artwork.json`，截图字节来自实际 Widget 渲染，不替换产品图片组件。
- 修订后的首轮 86 通过、1 失败，原始像素断言均已通过，但显露缓存管理器构造时的路径插件异步初始化问题。测试补充独立临时目录、路径插件夹具、真实缓存元数据初始化与显式释放；保留全部原像素断言，不修改产品来规避测试环境。随后八文件 **87/87** 通过。
- 新回归覆盖：六组移动/web 文件名、非目标 URL、移动与 web 两种帧位置、普通高图居中对照、真实卡片与圆形关注头像像素、收藏记录往返、新/旧通用封面缓存；相邻分页/分类切换/目录/收藏/猫耳适配/事务刷新/图片 URL 回归同轮通过。

87 项通过记录为 `20260907T194150878Z-area-artwork.json`。同轮五文件 analyze 只报测试文件冗余 `dart:typed_data` 导入（89.5 秒）；已删除导入，复用此前 87 项结果，仅重做范围 analyze；同时恢复既有 AreaCard 的 CRLF 换行，保留 Dart 行内容。

最终五文件 **analyze 无诊断，170.2 秒**，记录 `20260907T195412230Z-area-artwork.json`，整体 634.901 秒包含等待其他重任务及资源收尾。系统重进程峰值 CPU 61.69%、工作集 27,089,190,912 B；记录结束仍有 2 个系统重进程活跃，本轮验证命令已正常退出、监控和互斥已释放，没有停止其他任务。它不是“整台电脑已无重进程”的证明。

此次不为单个图片修订重复打包；保留版本 `3.1.8+4121`，没有新 APK/Windows 包、安装或发布。下一批累计候选再做 Android/Windows 实际加载、收藏与头像视觉复验；本轮 Widget 像素证据不充当设备验收。最新包仍为 Android `7aaecb8e`、Windows `f3de664a`，均不含此图片修订。全目标与 3.2.0 发布仍未完成。

### 后续累计构建

上述为图片源码批结束时状态；图片修订现已进入[58546f51 Android 累计候选](DIRECTORY_RECOVERY_AUDIT_2026_09_08.md)，已完成包内容和 SHA 核验，尚未安装。Windows 新包与原生视觉复验仍待继续。
