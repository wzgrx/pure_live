# 辅助页面 Android 原生补证（2026-09-07）

复用已安装 `af88a032f53e389ea4260ae916f53ea73f53f393` Android arm64 Debug，不重复构建。上轮完整门禁/安装/首页横屏见 [累计候选审计](CUMULATIVE_CANDIDATE_AUDIT_2026_09_07.md)。本轮先只读复核 `25102RKBEC / myron`，所有设备操作明确 `-s 192.168.1.2:5555` 并经 `run_android_device_test_turn.ps1 -NoRotation`。

## 实际完成与边界

| 子场景 | 本轮证据 | 结论边界 |
| --- | --- | --- |
| 历史页入口与列表 | 菜单 → 历史记录，标题 `50/50`、双列卡片和两个工具按钮可见 | 当前候选真实页面已打开，不代表 50 个房间都在线 |
| 保留数量草稿取消 | 打开弹窗，50 → 20 草稿，截图出现“当前值: 20”，取消后仍 `50/50` | 未确认缩减上限，不截断用户记录 |
| 清空确认取消 | 仅打开清空确认，点击取消后仍 `50/50` | 没有执行清空或删除操作 |
| 下拉刷新 | 真实下拉后捕捉“正在刷新…”与更新时间，后续刷新提示退出，标题仍 `50/50`，页面可返回 | 未录下结束瞬间的成功/失败提示；不声称全部站点请求成功或全量身份集合已逐项核对 |
| 工具箱入口 | 首页更多 → 链接访问；真实页面两组输入、操作按钮和支持列表可见 | 本轮初始字段为空；不由此推断剪贴板具体内容或自动填充已全面通过 |

证据目录 `local-artifacts/auxiliary-native-af88a032-20260907/`；PNG/XML、各轮日志、`verify_evidence.py` 和 `result.json` 保留本地。图像与语义交叉检查确认上述五个场景，没有将脚本退出 0 直接当作整体功能通过。

## 输入前拦截与清理

准备测试工具箱空输入时，前台守卫在发送点击之前失败；没有产生 quick PNG 或输入事件。随后的只读观察发现前台为 `tv.danmaku.bili`，Pure Live PID 6243 仍存在。不推断前台切换来源，也不据此把应用判定为崩溃。为了不干扰其他测试，本轮不抢回前台。

工具箱空白/非法输入、输入法、折叠草稿、真实平台直链选择/取消及播放器相邻入口继续未执行；不是失败后忽略断言。清理只停止 Pure Live，其他应用未操作；包装器均释放临时常亮，本轮没有改变旋转、网络、Root/LSP、ADB 端口或调试授权。日志采样命令未留下错误日志文件，不把缺失文件记为“日志无错误”。

## 本轮发现的文案问题

原生保留数量弹窗显示“自定义输入尺寸”，英语同一 key 为 `Custom Dimensions`；预设标题则复用“当前已启用的可选项”。这些是数量设置，不是尺寸或启用开关。源码 `HistoryPage` 复用 `custom_input` / `current_options`；冻结上游 `c6c9bd70aedc503c003110dae10a83ad0bb891d8` 同样使用这两个 key，记为 **upstream-existing**，本轮没有合并上游。

修订只在历史弹窗使用新的 `history_limit_presets` / `history_limit_custom`：中文“预设数量／自定义数量”，英文 `Preset Limits / Custom Limit`。原通用翻译保持不变，避免改变实际尺寸设置等其他页面。没有更改上限算法、草稿保存、刷新或 Hive 格式。

新增中英文实际 Widget 检查：320×480 双倍字体下显示正确数量文案、不出现旧尺寸文案，取消后上限与记录保持；既有历史交互、数据和公共刷新提示一并回归。最初命令误填了不存在的 `history_settings_test.dart`，入口在执行 analyze/测试前退出；核对真实文件后使用 `history_metadata_test.dart`，不把命令拼写错误当产品红测。

本轮文案修订及相邻历史/刷新 **43/43 定向回归通过，analyze 无诊断（126.6 秒）**。记录 `20260907T022905531Z-quality-focused.json`，基于 cb66ae2f 的本批工作树，249.483 秒，结束活跃重型进程 2；重型步骤依共享资源守卫串行，没有停止其他项目进程。日志 `local-artifacts/history-count-labels-final-20260907.log`。已安装 af88a032 仍是原文案，不把源码修订说成已经更新手机。下一阶段优先对齐设备使用窗口后补工具箱/播放器原生步骤，并继续其余辅助页面、Windows、录制和性能工作组；总目标与 42 个历史未闭环大项保持不变。
