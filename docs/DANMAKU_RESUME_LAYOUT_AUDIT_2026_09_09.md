# 弹幕恢复按钮边界审计（2026-09-09）

## 结论与范围

源码提交 `f35e60576cc92dee86ab390ef3f15148bfad2c20` 修订弹幕列表恢复按钮的宽度约束。4 个真实本地化 Widget 布局红项转绿；四文件 **19/19** 定向测试、两文件静态分析通过。对应 A4-02 的一部分，不提升完整原生验收状态。

没有构建、安装、发布或操作手机；Android 已归档候选仍为 `bee143e21664e8222b8edabab6aa5ea7d5a87850`，**不含本次 UI 修订**。Windows 候选仍为 f3de664a。全平台 3.2.0 正式交付继续按完整目标暂缓。

## 根因与来源

基线 `bea90e963f822efc23d2302e0c7b22884b65c0b4` 中，`DanmakuListView` 暂停追尾后显示 `Positioned(right: 12, bottom: 12)` 按钮，没有横向宽度上限。新消息累计数的中英文长文案在窄视口或大字号下令按钮左侧越出列表，随后被父级裁剪。第一处错误是布局约束，不是消息计数或恢复状态机。

分类 **upstream-existing**：冻结上游 `c6c9bd70aedc503c003110dae10a83ad0bb891d8` 存在相同布局，blame 的 ed8011b6 为其祖先（退出 0）；merge base 为 `527fea1b40885e3621d53c9646b523dd8522290c`。仅本地局部修订，没有合并或同步上游。

## 有效红测

新测试加载实际 zh/en JSON，挂载生产列表，真实拖动暂停，从 60 条历史追加 137 条消息；断言冻结行保留、最新行未显示和完整计数文案。以下四组按钮左边界均小于要求的 8：

| 语言 / Widget 视口宽 / 字号比例 | 旧按钮左边界 |
| --- | ---: |
| en / 320 / 1 | -197.20001220703125 |
| en / 320 / 2 | -641.2000122070312 |
| zh / 320 / 2 | -163.50003051757812 |
| en / 900 / 2 | -71.70001220703125 |

独立的冻结、一次恢复、再次暂停后新计数流程在旧实现已通过。因此红测结果为 1 通过、4 布局失败，不把正常状态机描述为故障。测量使用 Flutter Widget 测试字体/坐标，不是实机像素或截图结论。

## 修订与相邻验证

给 Positioned 增加 `left: 12`，并用右对齐 Align 保留短按钮的紧凑宽度。长文案在有限宽度内换行；字号、完整计数、样式和唯一显式恢复入口保持。没有更改历史上限、冻结快照、消息去重、Timer、PiP 呈现恢复、播放器或网络控制器。

四组测试核对左右及底部边界、至少 48 高度、命中测试和真实点击后的最新尾行/按钮消失。独立桌面普通字号用例核对短按钮不拉满、第二次拖动后的计数从 1 开始。绿测还加强了新消息到达时滚动偏移不变的断言；四个原红项的左边界断言原样保留。相邻滚动策略、到达计数、冻结屏蔽三个文件同时回归。

隔离夹具无真实账号、网络或播放器；卸载后关闭流与临时 Hive。既有 frozen-filter 测试的未加载文案警告保留，新增布局用例使用真实双语资源，测试及分析退出均为 0。

## 可复核证据

- 原始目录：`local-artifacts/danmaku-resume-20260909/`，含 baseline.json、red/green 脚本、日志及源码哈希。
- 红测记录：`local-artifacts/build-records/20260908T210104138Z-danmaku-resume-red.json`，258.167 秒，测试退出 1，结束活跃重型进程 0。
- 绿测记录：`local-artifacts/build-records/20260908T211204803Z-danmaku-resume-green.json`，513.055 秒，19/19、analyze 无问题；峰值 CPU 60.68%、Working Set 12,258,516,992 B，结束活跃重型进程 0。
- 绿测实际输入 SHA-256：生产文件 `256F8C769D2ABBA76B65583FF7AB6B1CA673D56E3350B0C69E2E47B3C10C3438`；新增测试 `99DDA149E8AFAF967304604A3AF19E698E01ED044790A5CA52C3EC8C53D8F9C0`。提交前逐项一致、diff 检查通过。
- 生产文件：`lib/modules/live_play/widgets/danmaku/danmaku_list_view.dart`；新测试：`test/danmaku_list_resume_test.dart`。

## 剩余验收与回滚

下一步将此修订纳入下一次累计候选，并核验真实 Android 中英文/大字号、横竖屏/短高度、小窗以及 Windows 窄窗口/滚轮，不把 Widget 的 640 高视口外推到所有高度和字体。快速滚动、关键词管理及 120 Hz 长期呈现按原矩阵继续；历史实机证据见 [列表累计数审计](DANMAKU_LIST_AUDIT_2026_09_06.md)。

宏观 42 个未闭环项、10 组未注册参考平台、bee143e2 的 38 组候选原生场景保持原口径；这些范围重叠，不相加。回滚为单独反向提交 f35e605 的生产布局改动，恢复基线；没有配置或数据库迁移。原候选及用户数据保持。
