# 克拉克拉萌星目录遗漏与暂缓候选（2026-09-08）

## 当前结论

源码 `8d5883f10272c9fcfba387c72cc37ef8e69cdbb5` 已修订萌星推荐目录被过滤成空页的问题，并纠正分类标题。**4 个有效复现失败项转绿，七文件 224/224 定向测试、三文件 analyze、真实生产适配器探针 1/1 通过**。全目标、原生验收与正式 3.2.0 尚未完成。

此前原进程完成了 `8ea62ac6` 的完整门禁与 Android 构建，但该包不包含这次修订，明确标记 **hold-known-kilakila-directory-bug：暂缓安装**，只保留诊断/回滚链路证据。没有手机操作、数据清除、模块更新、上游同步或发布；版本仍 3.1.8+4121，原生 PASS 数量不变。当前源码仍为 15 个直播站点 + IPTV，12 组参考平台未注册。

## 复现、来源与第一个错误状态

- 源码基线 `8ea62ac6ae6389dcff7ce564916672620c687919`。02:07 UTC 匿名请求官方 `/pcLive/timeline`，type=107、pageSize=10，第 1/2 页各 10 行，均为 **dataType=2**，isLastPage=false；检查的房间具备匹配 UID、字符串广播 ID、status=4、goldPrice=0。没有下载媒体或使用推流字段。
- [官方首页模块](https://live.kilakila.cn/static/pclive/js/chunk-5b86f1f0.7d0cbe13.js)将 `newList` 显示为 **萌星推荐**，以 type=107 查询，卡片按 roomResq/userResp 展示；不是由内部 `getNewLiveList` 方法名推断出的“新人直播”。复用已取得的 30,289 B 官方脚本，SHA-256 `d78b25452da819df07f59d0d34cf9cfcf171c6120b00d49d7fdbb3bdb4b6ce2a`。
- 第一个错误状态在 `KilakilaApi._directoryRooms`：所有非 8 行在身份/结构验证前被丢弃。随后应用拿到 rooms=[]、hasMore=true，表现为空目录/继续寻找下一页；不是官网没有直播的证据。本批证明 API 和实际站点映射错误，未声称已在手机上复现界面。
- 分类 **fork-regression**：自建 API 首次加入提交 `4febecafffd57898977c155762669b7db5bdd147` 使用仅 8 的过滤规则；应用提交 `d4eae7da` 注册了 107 分类却沿用了错误假设。只读冻结 upstream/master=`c6c9bd70aedc503c003110dae10a83ad0bb891d8` 的对应目录为空；无 fetch/merge，未声称这些是上游当前最新状态或外部平台刚刚变更。
- 上一版探针只断言热门列表有房间，允许 107 分类为 0 条并记录成功，因而 **2081 项全量测试没有覆盖本次真实响应种类**。这是验证范围缺口，不把既有绿项外推成完整目录能力。

## 修订与回归

1. 仅对已验证的 type=107 额外接收 dataType=2，保留原有 8 行支持；热门目录与推荐接口仍保留原过滤合同，未全局放宽陌生行类型。
2. 所有接受的行继续执行完整 roomResq/userResp 结构、UID/广播身份、状态和价格验证；未知状态与媒体处理规则不变，目录不携带播放地址。
3. 分类 ID=107、areaType=timeline 和原翻译键保持，避免设置/分类收藏迁移；中英文标题改为“萌星推荐”/“Rising stars”，目录说明同步。
4. 增加六项确定性合同用例：真实行种类、实际站点 UID 映射、错配身份、热门合同隔离、陌生行排除及官方标题。源码修订前 **2 通过、4 失败**，修订后纳入 224 项全部通过。最初测试脚本多一个右括号的语法错误已先修正，不把它计为有效 Bug 复现。
5. 生产探针现在要求存在可验证的萌星在播样本，而不是允许空结果也宣布该目录通过。02:29:08 UTC 实际得到热门 10 条、萌星 10 条，均 hasMore=true；同 UID 元数据、FLV/HLS、播放/恢复/录制地址解析成功。仍为匿名直连 Dio，未覆盖应用代理、媒体解码、录制成品或原生 UI。

最终三文件 analyze 无诊断，71.9 秒；中英文 JSON 与 `git diff --check` 通过。最终资源记录 `20260908T022920449Z-kilakila-rising-star-green.json`：453.156 秒，采样峰值 CPU 63.89%、工作集 15,261,159,424 B，结束活跃重型 0。采样集合不作为应用运行时性能结论。

## 已构建但暂缓使用的 APK

| 项目 | 实际证据 |
| --- | --- |
| 源码 | `8ea62ac6ae6389dcff7ce564916672620c687919`，构建时干净；不含本次修订 |
| 质量门 | 2081/2081 测试、42/42 既有接口探针；一次全量 analyze，151.0 秒、无诊断 |
| 质量记录 | `20260908T015808040Z-quality-full.json`，421.888 秒 |
| 构建记录 | `20260908T021644423Z-build-androidarm64-debug.json`，共 1558.891 秒（包含完整门禁及资源排队）；assembleDebug 435.4 秒 |
| 包 | Android arm64 Debug，com.mystyle.purelive，3.1.8+4121，Manifest code 6121 |
| 大小 / SHA-256 | 288,142,962 B / `0CA2EB746A311882CA4FA274F5B6336585D2753F701EA1BA9265B6DED0C2D717` |
| 内容 | 16 ELF 与 ZIP 的 16 KB 对齐、Flutter 资源、翻译键及无本地 TLS 私钥检查通过 |
| 签名 | `1e832295a696cf8210bca063e458bad0be12dfa64939d3362ff11b8f237ff7b9`，与先前 3a7716b0 Debug 候选相同；不是对手机当前安装签名的重新确认 |
| 独立归档 | `local-artifacts/candidates/android-8ea62ac6/`；复制后 SHA 一致，原 3a7716b0 保留 |
| 就绪状态 | 暂缓安装、未发布、没有原生 PASS 增量 |

原执行会话 84441 始终沿用，未因排队/观察超时重开。发现缺陷后曾准备仅取消本轮排队进程，但取消前检查发现它已创建 Flutter 构建子进程，因此没有发送终止命令；保持源码冻结至构建和归档结束。其他工作区进程未终止。

首轮归档把 Java 的 stderr 警告误当 PowerShell 终止错误。只修正本地归档辅助脚本的诊断流处理，仍强制检查 apksigner 退出码；随后新旧 APK 均完成证书核验。保留 `021736745Z` 失败及 `021851860Z` 成功归档记录；这不是 APK 密码学校验失败后跳过验证。

## 下一步与证据边界

本批在 `local-artifacts/kilakila-candidate-20260908/` 绑定 9 个源码输入、32 项证据；包含原流水线、队列状态、实际 HTTP 分类、官方脚本来源、红绿测试、探针、暂缓标记与归档信息。测试后哈希与提交 Blob 再核对。

**下一次必须从本次修订后的干净源码重新执行完整门禁并构建候选**，不把旧包或旧 2081 项结论当成新源码通过。随后按实际设备窗口验证目录、UID 收藏和双协议原生录制；长期、代理、其余平台及全平台稳定发布仍未闭环。Windows 最新候选仍 f3de664a，未在本轮重建。
