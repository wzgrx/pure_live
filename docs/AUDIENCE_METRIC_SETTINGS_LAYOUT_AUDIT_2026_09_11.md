# 观看数据设置平台完整性与大字号布局审计（2026-09-11）

## 范围与基线

- 基线：`6db89c06ba4bcbaa4cd4afd95c7d934d8318149d`。
- 对应验收组：A2-01 的观看数据二级设置页增量。
- 固定矩阵：`320×480`、英文、3.0 倍文字；同时保留 `420×900` 中文普通字号回归。
- 操作链：切换“Prefer concurrent viewers” → 逐项滚动 21 个普通直播平台 → 核对英文站名和说明边界 → 关闭 AcFun 在线人数开关。

## 修改前复现

新增回归在旧页面上执行时结果为 **1 PASS / 1 FAIL**：页面只声明 14 个站点，测试滚过已有站点后仍找不到
`missevan`，因此在有界滚动结束时报错。与 `Sites.supportSites` 对照后还缺少 Inke、Kilakila、Huajiao、
Xiaohongshu、niconico 和 Weibo Live，共 7 个当前已注册普通直播平台。

旧表的 Bilibili、Douyu、Huya、Douyin、Kuaishou 与 NetEase CC 名称直接写成中文；英文页面仍显示中文站名。
平台说明固定放在 `SwitchListTile` 的三行副标题内，两个全局模式也固定使用标题/副标题槽位，没有覆盖窄屏大字号的
完整文本边界和逐项可达性。

## 修订

- 设置页平台清单与当前 21 个非 IPTV 直播站点对齐；支持并发人数的平台保留可切换开关，其余站点继续显示关闭态及真实数据来源说明。
- 所有平台名统一通过既有 `site_*` 本地化键解析，移除英文界面的硬编码中文名称。
- 为 Missevan、Inke、Kilakila、Huajiao、Xiaohongshu、niconico 和 Weibo Live 增加中英文观看口径说明；
  热度、累计观看、展示文本和未知值均不混入并发在线排行。
- 两个全局模式和全部平台行在窄屏或大字号下把说明堆叠到标题下方，由内容自然撑高；常规宽度继续保留紧凑标题/副标题布局。
- 回归逐项滚过 21 个站点，验证英文名称、说明底边不越出自身设置行、模式写入和 AcFun 开关响应。

## 验证

最终统一质量记录：
`local-artifacts/build-records/20260911T050419341Z-quality-focused.json`。

- 观看数据设置、观看口径模型/排行、CC/Douyin/Huya 适配、设置持久化、站点注册与本地化契约九个测试文件共 **44/44 PASS**。
- 全库 `flutter analyze` 执行一次，**No issues found**。
- 仓库完整性审计错误为 0；质量记录结束时仍观察到 1 个非本批持有的重型进程，实际 ADB 命令为 0。
- 本批未操作手机、构建候选、修改版本或发布 3.2.0。

## 当前边界

A2-01 保持 `RUN`。本批闭环 Windows 确定性窄屏/大字号、双语站名、普通平台清单和设置写入；
Android 触摸、返回、进程重启后的设置恢复以及当前最终候选仍按总验收矩阵执行。
