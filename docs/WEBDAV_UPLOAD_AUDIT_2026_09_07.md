# WebDAV 上传与 HTTP 传输审计（2026-09-07）

## 基线、来源与复现

- 基线 `bce52b82`。只读对照冻结上游 `c6c9bd70aedc503c003110dae10a83ad0bb891d8`，同样拒绝 `/` 上传、按秒生成固定文件名且未设置在途门禁，归类 **upstream-existing**；没有合并或宣称已查询远端最新状态。
- `/` 表示用户配置的 WebDAV 基地址，并非一律是服务器禁止写入的顶层空间。当前页面在根目录显示可点上传按钮，但控制器在调用服务前直接返回；用户把基地址设为可写备份目录时也被阻断。
- 真实页面红测：根目录点击后的写请求数期望 1、实际 0。首轮记录 `20260906T193208605Z-quality-focused.json` 为 13 通过 / 2 失败；另一用例在初始根目录请求尚未完成时直接改了路径，导致其加载状态悬挂并让 `pumpAndSettle` 超时，这是夹具时序问题，不作为重复上传已动态复现的证据。修正为先完成初始浏览再切换目录。
- 源码可直接验证：连续调用在首个 await 前没有互斥；相同秒级时间得到相同远端文件名。锁定客户端通过 PUT 写入该路径，存在覆写先前备份的风险。

## 修复设计

- 根目录和子目录均向服务提交上传，是否可写由实际服务器响应决定；不创建特定供应商例外，不更改用户配置地址。
- 每个当前服务仅允许一次在途上传。按钮禁用并显示上传指示与中英 tooltip；控制器本身也检查状态，覆盖同一帧内直接重复调用。成功/失败后恢复入口，可显式重试。
- 第一轮修复回归另行复现底部 Get snackbar 遮挡 FAB：360×640 窗口点击 `(316,596)` 命中提示而非按钮，连续备份与失败后立即重试均仅产生 1 次请求。冻结上游的同一 SnackBarUtil 也使用底部独立 overlay。上传反馈局部换为 ScaffoldMessenger floating SnackBar，让 Scaffold 按 FAB 布局避让；未修改全局提示工具，也未通过额外等待或关闭点击告警隐藏问题。
- 初始化新服务释放旧服务对应的上传状态；旧请求的 finally 仅在仍拥有原服务时更新状态，避免清除新服务正在上传的标记。沿用已有目录/服务代次检查，未承诺撤销服务器已接受的操作。
- 文件名保留可读秒级时间、`purelive_` 前缀与 `.txt` 格式，并增加 UUID v4，避免普通同秒连续备份及多客户端时间重合导致相同路径。没有宣称数学上绝对无碰撞，也未新增条件 PUT 协议或重写客户端。
- 未更改备份导出的敏感字段默认省略策略或恢复结构。

## 验证

- 首轮修复后 **61 通过 / 2 失败**（`20260906T193749886Z-quality-focused.json`），analyze 无诊断；失败集中在上节提示遮挡按钮，根上传、重复点击门禁、服务代次和实际 HTTP 往返均通过。
- 最终 **63/63 通过**：模型 2、真实页面 17、控制器 29、目录 HTTP 服务 6、新增传输 HTTP 服务 3、真实 Hive 备份恢复 6。analyze **No issues found，113.7 秒**；记录 `20260906T194223143Z-quality-focused.json`，日志 `local-artifacts/webdav-upload-final-20260907.log`。
- 门禁对应 `bce52b82` 加本批工作树，193.267 秒，结束活跃重型进程 0；之后仅完善证据文档。最终日志没有按钮 hit-test 警告。
- 新增真实页面覆盖根上传、重复点击禁用、固定同一秒连续备份与失败后立即重试；控制器覆盖按钮重建前重复调用及新旧服务上传状态隔离。新增 loopback HTTP 服务用实际锁定客户端执行 PUT/GET/DELETE 往返，覆盖配置基地址、中文空格子目录、根写入 403 保留已有数据；只操作内存夹具，不是用户云盘写入或删除验收。

## 待完成

- 本批尚未构建或原生验收；既有 `b5ab680d` Windows 目录候选不包含本次上传改动。
- 下载/删除在途交互、传输大小/取消、更多真实供应商兼容和外部设置同步继续检查；不将这些窄范围证据认作 WebDAV 或全平台验收完成。
- 3.2.0 未发布。回滚为撤销本批代码与测试变更；旧备份保留，新文件仍为兼容的 JSON 文本格式。
