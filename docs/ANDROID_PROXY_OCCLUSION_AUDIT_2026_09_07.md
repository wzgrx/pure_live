# 浮动播放器遮挡下的代理收尾（2026-09-07）

接续[诊断候选原生失败](RECORDING_DIAGNOSTIC_ANDROID_AUDIT_2026_09_07.md)，基线 `3a6d293b`。本批只修改设备测试工具与覆盖率报告；复用手机已安装的 `1aa6886f` Debug，不改变应用、重构建、升版、合并上游或发布。

## 复现与修订

旧脚本对真实 `proxy-ui-5.xml` 点击节点中心(600,399)，落在浮动播放器[390,300][1050,671]内。离线红测实际得到旧坐标，而期望未遮挡矩形中心(219,399)，记录 `local-artifacts/proxy-occlusion-20260907/red.log`。

- `7c0584b7`：从目标可见bounds中扣除其他分支可点击区域，目标边缘与遮挡边缘各留8px，保留有足够内部面积的候选；原中心可用则保留，否则选最大未遮挡区域。祖先不误判为遮挡；完整遮挡、无内部空间、异常bounds、复杂几何停止输入。不关闭或拖动浮窗、不猜绘制层次。
- 每次计划写入 `proxy-tap-N.json`，只记录bounds/点位/排除数，不含页面账户或凭据。status=planned仅表示计划；实际成功需后续界面/开关状态证明。输入前原有精确前台检查仍执行。
- 代理入口只点一次；随后最多3次新XML观察确认代理开关出现。没有打开目标页时停止，不重复点击或在旧设置页继续滑动寻找另一个页面的开关。
- `0f438541`：首次实机遇首页折叠控件[1080,2080][1080,2080]，旧的严格bounds处理阻断菜单点击。零面积非目标控件没有点击区域，现忽略；目标零面积、格式异常或倒置区域仍停止。
- `c320b5b5`：第二轮浮窗下菜单的“关闭菜单”全屏背景从祖先变为兄弟节点，保守遮挡规则使其阻断“设置”。仅对本应用的原生MenuItem与唯一、同包、完整覆盖的明确“关闭菜单”背景角色组合进行识别。任意无标签背景、非MenuItem目标继续阻断，不泛化为忽略全屏遮挡。

两份真实XML夹具只含通用菜单/设置语义，来源及限制见 `tool/tests/fixtures/README.md`。同一窗口几何不能证明所有Android命中顺序；本批用下面实际路由及开关结果补证。

## 门禁及未掩盖的失败

- 最终 **38个代理事务场景通过**（原25+新增13），包含真实遮挡点、零面积、菜单背景角色、完整遮挡、无效几何、单次导航无提交、前台丢失与既有映射所有权。
- 73个录制守卫场景通过，国外代理包装器6/6、设备轮次包装器7/7通过，静态构建策略与diff检查通过。包装器输入未变，后续两次几何修订复用包装器证据；最终录制守卫再次执行。没有Flutter analyze/应用全测或APK构建。
- 多遮挡测试初版选择的间隙仍允许原中心，预期错误；调整几何使原中心确实受遮挡后通过，保留初版失败日志。不通过修改正确算法迎合错误坐标期望。
- v1 (`foreign-proxy-session-f8b29aa2ef5a40e2ade7c760db161067`) 在首次菜单输入前遇零面积控件退出；尚未创建session/reverse或切换代理。常亮释放，没有录制。
- v2 (`android-recording-smoke-20260907T181304774`) 完成33.384666秒短录，但自动代理恢复在菜单背景识别处停止，整轮FAIL。自己的reverse已移除；随后明确从MT启动本项目并恢复原开关，独立恢复成功。没有在前台守卫失效后继续输入。该录像10,910,740 B，SHA `C95455BD0BBA1A05E8B07235A998F2A106227F76A84A99AAC95CB7838AD7B427`，严格全部音视频解码退出0、错误日志0 B。

## v3实际完整收尾：通过

工具源码 `c320b5b5`，应用仍 `1aa6886f`。证据 `android-recording-smoke-20260907T182036955`，包装器 `native-wrapper-v3.log` 退出0：

1. 本轮正常创建自己的7897 reverse、启用两个应用内代理开关；没有改ADB端口/Wi-Fi。
2. 实际Picarto首房间为FattyNite，只有1080p60与线路1，切换质量/线路记SKIP；远程弹幕尚未接入。
3. TS从262,144增长到524,288 B；墙钟33.366秒，MP4 23.632667秒 / 2,685,657 B，H.264 1980×1080、标称60fps及音频。实际平均帧率与标称值不同，不宣称恒定60fps性能验收。SHA `8E93A62C090438FFD420E9ED7E593D03F2098377E0043DCC66F9831588664EBF`。
4. 主机使用 `-xerror -err_detect explode` 完整解码所有音视频，退出0、错误日志0 B；本轮两份文件串行解码结果为 `decode-results.json`。
5. 浮窗保留时，实际菜单点(204,384)，代理入口从被遮挡中心改到**(219,399)**，代理应用层开关改到**(219,523)**。后续新XML及开关状态证明目标页进入和状态提交，不只依赖计划日志。
6. **proxyRestoredBeforeStop=true**，两个开关恢复原false值，监控取消，自己创建的reverse移除，进程/本应用唤醒锁清理成功；无FATAL/ANR、无前台干扰。外围finally对已恢复session再次核验成功，没有手工补救。

session `foreign-proxy-session-85dbb3e2485849469c78e133035d13c5/session.json` 在10:24:27 UTC为restored，ownedReverse/reverseUncertain/uiMayHaveChanged=false。10:26:02 UTC再核验身份25102RKBEC/myron、进程不存在、7897 reverse不存在、本应用唤醒锁不存在。包装器finally StayAwake=false。未调用Root、MT MCP、lspctl；没有手机/adbd重启、卸载、清数据或模块更新。

## 诊断及覆盖率边界

本次原生停止快照stopElapsedMs=8056、HLS预算8000、forcedCancel=true、inputDrained=false、inputIntegrityError=false，而严格全解码健康。**强制取消不等于媒体损坏，健康文件也不证明停止逻辑所有边界已关闭**。不因本条记录盲目延长超时；旧坏录像原因仍待定位。封装日志包含已知时间戳哨兵，终态文件时长以ffprobe为准，不把极大统计值当作录像长度。

发现原报告把“平台未实现弹幕”的条件豁免布尔值显示为PASS。`d14dfcbc`将未实现的连接/聊天检查标记SKIP，保留支持平台真实PASS/FAIL，且不修改原断言。新增覆盖率回归通过；首个调用包装器读取了遗留LASTEXITCODE而误报失败，独立pwsh进程运行复验退出0。原实机summary原样保留；`coverage-reclassified.json`明确标注只重新分类已有证据、未重跑手机。

本批关闭的是浮窗条件下这条代理自动收尾子项及其错误的测试覆盖率表述。Picarto长录/后台/签名续接、其他平台、Windows及全目标3.2.0验收仍继续，历史42个RUN/NR大项不因此机械减一。下一步回到平台能力扩展与录制边界，不重复构建同一候选或反复跑已验证的代理往返。
