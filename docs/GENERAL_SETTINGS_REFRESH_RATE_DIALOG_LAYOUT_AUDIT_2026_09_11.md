# 通用设置刷新率弹窗窄屏大字号审计（2026-09-11）

## 范围与基线

- 基线：`8c95fee206f9285bf6221073688d34fa3028047f`。
- 对应验收组：A2-01 的二级设置页增量，以及 A2-02 已通过刷新率策略的呈现回归。
- 实际执行环境：Windows；视口 `320×480`、英文、3.0 倍文字。
- 操作链：通用设置 → 界面刷新率 → 滚动到最高性能档 → 选择并关闭弹窗。

## 修改前复现

干净红灯记录：
`local-artifacts/build-records/20260911T021015579Z-quality-focused.json`，结果 **0/1 PASS**。

固定横排的档位名称和耗电徽标在每个 `RadioListTile` 只有 144 像素标题宽度时，分别稳定右溢出
**174、274、207 像素**；`SimpleDialog` 的完整内容又向底部溢出 **248 像素**，最高性能档未进入
可操作树。刷新率策略本身未失效，问题位于弹窗布局和可达性。

## 修订

- 将刷新率选择器切换为显式可滚动的 `AlertDialog`，标题、说明和三个档位共享同一有界滚动区域。
- 档位名称与耗电徽标由固定 `Row` 改为 `Wrap`；宽屏保持同排，空间不足时自然换行。
- 保留原 `RadioGroup`、即时保存、关闭弹窗和三档刷新率语义，不更改 Android/Windows 平台策略。
- 新增 Windows Widget 回归，要求六个名称/耗电标签全部构建、最高性能标签滚动可达、点击后状态变为
  `AppRefreshRateMode.performance` 且弹窗关闭。

## 验证

最终统一质量记录：
`local-artifacts/build-records/20260911T021958255Z-quality-focused.json`。

- 新弹窗布局测试、应用设置序列化、刷新率作用域、弹幕刷新率联动和设置首页布局共 **17/17 PASS**。
- 全库 `flutter analyze` 执行一次，**No issues found**。
- 仓库完整性审计错误为 0，质量任务结束后重型进程为 0，实际 ADB 命令为 0。

## 当前边界

A2-01 保持 `RUN`，A2-02 保持 `PASS`。本批补齐的是 Windows 确定性窄屏/大字号操作证据；
Android 原生字体缩放、触控滚动，以及其余二、三级设置页仍按矩阵继续。本批未操作手机、构建候选、
修改版本或发布 3.2.0。
