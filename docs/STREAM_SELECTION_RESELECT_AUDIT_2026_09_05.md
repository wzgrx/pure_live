# 切换中重新选择当前源的竞态审查

## 基线与归因

- 基线：本仓库 `8b0b126c5fe07a791f60e55ae3c9e9650bdaf439`，本轮未合并上游。
- 分类：`fork-regression`。`git blame` 将提前返回条件定位到 `733932f91`；不归咎于平台接口或上游最新改动。
- 范围：Android / Windows 共用 `PlayerController.switchStreamSelection`，包括画质和线路选择。

## 首个错误状态

当前提交为 A，用户选择 B。在 B 解析 URL 或打开原生源期间，用户重新选择 A；原实现见 A 等于当前 UI 状态就直接返回，没有使 B 的选择序号失效。B 随后仍可打开并提交，最后生效的不是用户最后选择。

## 修复与边界

只有没有切换进行时，重复选择当前源才是空操作。存在进行中的选择时，回选当前源也产生新的选择序号，通过原有 PlayerManager 串行生命周期队列恢复源；旧请求在解析/打开后的序号校验处退出，不提交 UI。没有增加重试、定时器、并发解码器或固定等待。

保留服务端实际画质确认、相同源伪画质拒绝、签名续期、失败回滚与房间生命周期校验。已进入原生打开的旧请求仍可能短暂执行，修复保证最终顺序，不承诺零帧切换。回滚只需撤销本次控制器条件和相应测试，无数据格式迁移。

## 证据

- 新增两项确定性测试：解析中的画质 B 被回选 A 淘汰；打开中的线路 B 后串行恢复 A。
- 修复前：9 通过、上述 2 失败，记录 `local-artifacts/build-records/20260904T211840466Z-quality-focused.json`。
- 修复后：11/11 通过，记录 `local-artifacts/build-records/20260904T212317313Z-quality-focused.json`。
- 同轮 Analyze 无错误/警告、1 项花括号风格提示；随后已为该条件补齐花括号，未重复启动全仓 Analyze，最终发布门禁仍需复核。
- 真机的旧 Debug 包平台切换/短录证据不算此修复的新包复验。新包安装、回选与相邻模式检查仍待执行。
