# Windows Release 虎牙长录制验收（2026-09-07）

复用 [2d8e4e0c Release 候选](WINDOWS_RELEASE_CPU_AUDIT_2026_09_07.md)，3.1.8+4121 unsigned，独立实例 `record_release_2d8e4e0c`、PID 31316。本轮没有应用源码修改、构建、全测、版本递增或发布；开始时仓库 HEAD 为文档提交 7e063dda。手机身份只读核对为 25102RKBEC / myron，前台仍为 Bilibili，未抢占输入、安装或修改 MT/Root/LSP。

## 实际动作与文件

- 虎牙热门进入 **胖炸 / 84进80【联合巅峰赛】龙凤店**，播放器和录制中心均显示蓝光20M、线路1。视频与远端弹幕在开始、中途、十分钟后的截图中持续变化；没有做听音验收。
- 开始点击记录为 **05:12:20.1946027Z**，成功停止点击为 **05:24:06.269Z**，相差 **706.0743973 秒**。之前一次 AX 停止操作因缓存元素失效未执行，单独记为失败尝试；重新观察后通过实际坐标停止，不把尝试时间当作成功停止时间。
- 录制中心先后观察到 00:10:53 / 160.41 MB、00:11:03 / 163.91 MB、00:11:37 / 174.16 MB；停止时“处理中”00:11:51 / 178.94 MB，随后“已停止”**00:11:51 / 168.92 MB**。
- 中途文件增长，十分钟后有同一前缀 `_000000.ts`、`_000001.ts`、`_000002.ts`；停止后合并为单个 MP4。`RecorderConfig.defaultSegmentTime=300`，命令使用 segment muxer；这些五分钟文件段不是签名续接次数的证据。
- 返回播放器后视频和弹幕恢复，再退出直播间回到虎牙热门列表；资源采样完成后 Alt+F4 → 应用退出确认，PID 已消失。未删除测试记录或用户数据。

最终文件位于候选实例的 `AppData/record_release_2d8e4e0c/RECORDS/huya/胖炸/2026-09-07/13-12-21/20260907_131221_058.mp4`。

| 指标 | 实测 |
| --- | --- |
| 最终大小 | **177,121,160 B**，168.915901 MiB，卡片两位小数 **168.92** 一致 |
| SHA-256 | `B641EB6E35215BD96E682B5839A025024260A95A5C30E64F3190E7CDAA1B1E64` |
| 容器时长 | **711.805833 秒** |
| 视频 | H.264 High，**1280×720**，标称60 fps，42,703帧，711.774833秒 |
| 音频 | AAC LC，44.1 kHz双声道，30,652帧，711.796077秒 |
| 完整解码 | 本机 FFmpeg `-threads 2`，全部音视频解码退出0，错误日志0 B |
| 逐包DTS | 视频/音频非正间隔均0、>2秒间隔均0；最大间隔0.046989 / 0.053220秒 |

画质名称“20M”不等于固定分辨率或实测码率，本文件实际为720p，约1.99 Mbps总码率。媒体比起止点击墙钟多约5.731秒，与之前短样本方向一致，但源端预缓冲及起止记录开销尚未分离验证；不声称精确墙钟等长或零丢帧。

## 性能与清理

使用既有只读采样器，24逻辑核归一CPU；没有采样GPU。录制段全程停留播放器，未在采样中切换菜单或录制中心。解码验证在离页采样开始前结束。

| 场景 | 时长/响应 | CPU均值/P95 | Working Set 首→末 MiB | Private Bytes 首→末 MiB | 句柄/线程 首→末 |
| --- | --- | --- | --- | --- | --- |
| Release播放+弹幕+录制 | 600.646秒，61/61 | 4.3918% / 4.8676% | 400.7891→444.5234，峰455.7344 | 872.2617→845.6562 | 1762→1747 / 251→244 |
| 离页后虎牙热门 | 60.574秒，13/13 | 2.5173% / 2.7133% | 403.2578→335.5781 | 980.4531→711.2969 | 1687→1326 / 237→155 |

内存、句柄、线程有实际回落；列表CPU仍约2.5%，**不标记性能PASS**。此前 [同步无障碍查询干扰](WINDOWS_CPU_CONTEXT_AUDIT_2026_09_07.md)尚未排除，本轮也没有仪表测量查询率，因此这些是当前桌面环境下的实测值，不是无干扰基准，也不归因于某个观察工具或直接与不同直播内容的Debug数值比较。

两个采样会话及完整解码均退出0；应用正常退出记录为05:28:37.9885035Z。未终止其他任务、服务或桌面进程。

## 证据边界与剩余

本地忽略目录 `local-artifacts/windows-long-record-2d8e4e0c-20260907/` 保留进程/起止/增长/退出JSON、原始日志、两组CSV及汇总、ffprobe、逐包结果、文件哈希和完整解码结果。原生截图见本任务工具输出，签名URL不提交文档。

当前 `recorder_controller.dart` 对 native WUP FLV 在健康连接上仅预取后续凭据，不为刷新定时器主动切流；其余租约保留轮换策略。本Release标准输出没有可验证的 `RecorderLease` 预取成功链，故**≥10分钟录制及多段合并完整性子项完成，两次签名预取/续接仍待独立补证**，也未模拟断网、下播或恢复。

W3-05增加当前Release长录子证据但保持RUN；其余平台、Android长录、窗口/PiP/音频组合、DLNA、设置/数据、性能和全平台交付继续。历史账本仍20 PASS / 32 RUN / 10 NR，42个大项未闭环，不以一个平台的一次长录替代全目标。
