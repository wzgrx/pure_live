# v3.1.0 直播录制参考项目审计

审计日期：2026-08-31。参考基线固定为：

- [`biliup/biliup@c4dbf81c5d3d`](https://github.com/biliup/biliup/commit/c4dbf81c5d3dbae7036660ddc180f10eb183d102)（MIT）；
- [`bililive-go/bililive-go@37d1d0e7d7a1`](https://github.com/bililive-go/bililive-go/commit/37d1d0e7d7a18ef31d592f8bdb17089518d12900)（GPL-3.0）。

本审计提取可验证的状态机、故障边界和验收方法，不直接搬运不同许可证项目的实现。Pure Live 的播放器、平台适配器与移动端录制仍保留自己的 Dart/Flutter 架构。

## 1. 已采用或已经具备

| 参考能力 | Pure Live 当前设计 | v3.1.0 动作 |
|---|---|---|
| 每个平台独立解析直播状态和输入流 | 十个平台均实现 `LiveSiteRecordRoomResolver`；播放用的 UI 兜底不会伪装成录制状态 | 保留严格录制元数据合同，并以平台探针和短录双重验证 |
| 画质、协议、CDN 逐级回退 | `StreamResolverService` 按平台 `sort` 排序；斗鱼、虎牙支持延迟签名的单线路游标 | 验证失败线路只推进一个游标，续签保持已应用画质，成功首帧后才提交 UI |
| 长时录制分段、失败重连、结束收尾 | FFmpeg 调度器已有准备、录制、重连、停止、失败状态和累计输出指标 | 逐平台执行 30～60 秒短录；虎牙增加跨短签名租约的长录证据；停止后实读文件与进程/句柄 |
| Cookie 按平台域隔离 | 平台设置已分别维护 Bilibili、抖音、虎牙、快手、Twitch、SOOP、YY 等凭据 | 日志和诊断只记录“是否存在”，不输出 Cookie、签名 URL 或 Token |
| 自动化接口/E2E 门禁 | `tool/interface_probe.py` 覆盖目录、搜索、详情、播放、弹幕身份和人数语义；Flutter 有录制状态机测试 | 正式发布前跑完整探针、确定性测试和 Android/Windows 实机矩阵，三类证据互不替代 |

## 2. 本轮新增的协议韧性

`biliup` 当前基线修复了 Bilibili 畸形弹幕包导致解码任务终止的问题。对 Pure Live 的独立审计发现：既有解析器已经检查 16 字节包头、包长、剩余长度并捕获顶层异常，连接不会因为一次 Dart 解码异常直接退出；但压缩输出大小和单消息包数量仍缺少硬上限。

v3.1.0 因此补齐：

1. WebSocket 单消息编码大小上限；
2. 单消息内最大协议包数量；
3. zlib/Brotli 流式解压输出上限，避免高压缩比输入一次性展开为不受控列表；
4. 压缩包递归深度上限；
5. 零长度包、合法包后畸形包、过深压缩包之后连接继续解析的回归测试。

这些限制只丢弃当前不可信 WebSocket 消息；后续消息仍由同一连接处理，既有已解析弹幕也不会回滚。

## 3. 录制状态机采用边界

Pure Live 面向交互式播放器，不能照搬服务器录播器“无限重试”的默认值。移动端和桌面端统一采用以下边界：

- **准备中**：解析房间、画质和首条可打开输入；未写入文件前不显示伪造的 `0 B / 0M` 录制速度；
- **录制中**：文件大小、时长和速率只从真实输出文件采样，跨重连保持累计值单调；
- **短暂失败**：指数退避并刷新当前平台租约；同画质当前线路续签优先，随后才轮换线路/画质；
- **确定下播**：停止调度，不把平台网络错误误判为下播；
- **用户停止**：立即取消解析、续签和重连，等待 FFmpeg 收尾并释放文件句柄；
- **不可恢复失败**：保留最后错误类别、平台、画质、线路和脱敏诊断，禁止只显示“录制失败”。

## 4. 新平台实施与验收待办

参考项目还覆盖 AcFun、OPENREC、微博、YouTube、TikTok 等站点。新增一个首页入口并不等于完整平台支持；至少需要目录/搜索、房间状态、画质/线路、播放租约、弹幕、观看指标、录制和登录边界八项合同。用户明确要求评估并增加参考项目的平台，此项仍是当前任务待办，尚未完成；现有十个平台通过不代表本项完成。按可达性与维护成本逐站确定实施顺序，不以只加入口冒充支持。平台缺少公开目录、弹幕或需要登录时，能力声明必须如实区分。

后续候选按以下顺序评估：公开入口与区域可达性 → 登录依赖 → URL 寿命和 CDN → 弹幕协议 → 指标语义 → 录制合法收尾 → Android/Windows 实机。每个平台都需要独立探针和故障夹具后再进入默认导航。

## 5. 发布验收

- 参考项目的“支持平台”列表不作为 Pure Live 已支持证据；
- 接口探针通过不等于播放器/录制通过；
- 30 秒短录通过不等于短签名长时续接通过；
- 单元测试通过不等于 Android 后台、Windows DPI/任务栏和真实磁盘路径通过；
- v3.1.0 Release 只写入本轮取得证据的结论，未执行项继续保留在 `ACCEPTANCE_MATRIX_3_1_0.md`。

## 6. 3.2.0 新一轮逐源码核查（2026-09-05）

本节不改写前述历史基线。当前远程 HEAD：biliup `906e0f6fdb104d65989d12b76c9a6f02205384cb`，bililive-go `ef71711a7c573b013d82fec01ee8d0609ee36aca`。仅只读获取参考文件，没有合并上游 Pure Live。

- [biliup AcFun 适配器](https://github.com/biliup/biliup/blob/906e0f6fdb104d65989d12b76c9a6f02205384cb/crates/biliup/src/downloader/live/acfun.rs)：可借鉴游客会话与 startPlay 分离、Referer/UA 绑定、嵌套 videoPlayRes 解码。当前实现将 startPlay 的所有非 1 结果映射 Offline；本项目接入时须区分明确下播、认证失效、限流、未知/结构变化，避免关注卡片和录制监控误判。该实现选 representation 的最后一个 URL，不等于可靠的最高画质排序；本项目应保留稳定 ID、码率/尺寸与 CDN 维度，缺字段时明确未知。
- [bililive-go OPENREC 适配器](https://github.com/bililive-go/bililive-go/blob/ef71711a7c573b013d82fec01ee8d0609ee36aca/src/live/openrec/openrec.go)：可借鉴独立平台 URL 注册和状态读取；当前按页面正则匹配标题、状态和 m3u8，任意非 200 映射房间不存在，且正则限定地址以 m3u8 结尾。直接照搬会误处理 429/5xx、带签名查询的 HLS 与页面嵌套结构。应验证页面公开结构化数据、完整 URL、返回码分类、重试边界和媒体探针后再接入。
- biliup Picarto 文件读取遇到 GitHub HTTP 504，本轮未完成源码核查，不将失败读取记作已审查。

下一阶段先给候选平台建立脱敏 fixture：在播、明确下播、无权限、限流、5xx、字段缺失、签名 HLS、多个画质/线路和 URL 过期。通过解析与状态合同后再实现目录/搜索/入口、真机播放与录制。新增平台仍待实施，本节是设计审查而非功能交付。

### 3.2.0 后续实施进展

上述待办已推进到 AcFun 的生产 Dart 协议/房间/质量/续签/录制输入适配，18 项离线合同测试
及公开流输入探针通过。详细版本、方法、能力缺口和证据边界见
[AcFun 实施记录](ACFUN_ADAPTER_AUDIT_2026_09_05.md)。OPENREC 当前代理样本返回 403，
继续保留独立候选状态；AcFun 入口/分类搜索/远端弹幕说明和完整录制验收仍待实施。
