# 猫耳 FM 应用接入与原生录制（2026-09-08）

基线 `48d2f7b4aa7d8fde2f4aea9ee5739328a0baf3b6`，继续 [公开 API](MISSEVAN_STAGED_ADAPTER_AUDIT_2026_09_08.md)、[原生目录分页](MISSEVAN_DIRECTORY_UI_AUDIT_2026_09_08.md)及[分类收藏身份](FAVORITE_AREA_IDENTITY_AUDIT_2026_09_08.md)三项已验证合同。本批不合并上游、提升版本或发布；不是完整 3.2.0 验收结论。

## 应用路径

- Sites 常量、集合、菜单及单平台工厂注册猫耳；双语平台名称齐备。源码现在为 **13 个直播站点 + IPTV**，参考分组尚有 **14 组未注册**。注册数量不表示完整能力或原生 UI 全部验收。
- 目录沿用可选原生分页接口；分类收藏保留 catalog/tag 身份。配置迁移版本 6 只追加猫耳，保留顺序和已隐藏平台，再次禁用后不重新启用。
- 公开房间链接接入分享文本、网页房间解析及原站打开动作；只接受核验过的 `fm.missevan.com/live/{id}` 形式，不把音频作品、分类页、伪造主机或非标准端口当作直播间。
- 播放、多画面、音频和录制共用猫耳公开 Origin/Referer/UA，不导入其他平台 Cookie。生产录制解析器直接通过注册工厂获取适配器，保留 HLS/FLV 协议选择、同协议续租、刷新时间与硬失效时间。
- score 仍为热度，不加入在线人数开关；没有强制猫耳为纯音频，媒体轨道由实际输入决定。
- 原生搜索和网页搜索尚未核验，因此声明 unavailable，搜索页提供热门/分类/粘贴链接指引，不落到通用搜索网站。搜索汇总不再把该平台描述成已支持网页搜索。单画面 EmptyDanmaku 路径已有未接入状态文案，多画面明确不创建猫耳弹幕会话。

## 发现与修订

1. 首轮 `20260907T180021150Z-missevan-integration.json`：80 项通过，1 项失败。多画面采用平台排除名单，任何新 ID 自动判为支持弹幕。冻结上游 `c6c9bd70aedc503c003110dae10a83ad0bb891d8` 同样使用此默认；新增猫耳把旧假设暴露为接入冲突。改为现有八个平台的明确支持集合，并保留其回归。没有增加 WebSocket 请求或杜撰猫耳弹幕协议。
2. 扩大调用面验证后 `20260907T180350968Z-missevan-integration.json`：200 项通过，1 项失败，发现缺少双语 `site_missevan` 字段；补齐字段，平台菜单改用翻译键。两轮均未进入 analyze，不把失败记录当作通过。

## 验证证据

- 最终定向 **201/201 通过**，覆盖猫耳 API/接入、目录、分类收藏、四代新增平台迁移、分享链接、请求头、热度、搜索及多画面；**19 文件 analyze 无问题，80.0 秒**。记录 `20260907T180934378Z-missevan-integration.json`，总耗时 286.252 秒，峰值 CPU 48.41%、工作集 16,109,686,784 B，结束活跃重型进程数 0。
- 公开探针 **1/1 通过**（UTC 18:10:42）：6 个分类入口、分类页 11 房、热门原生第一页 21 房/第二页 20 房，生产注册工厂与录制解析、HLS/FLV、同协议恢复、失效元数据及不存在房间分类通过；并非把服务端额外行截成 20 行。
- 原生 HLS、FLV 探针各 **1/1 通过**，两次增长采样均覆盖 26 秒、各 3 个分片，正常停止后提交 MP4 并移除源分片，完整成品 `-xerror` 严格解码退出 0。两者均为 AAC + 16×16 H.264；没有据此声称纯音轨或所有房间均验证。

| 输入 | MP4 时长 | 成品大小 | SHA-256 |
| --- | ---: | ---: | --- |
| HLS | 27.965333 秒 | 714,751 B | `D4BCB128B0D9D8FB975553D0CA2ABB8F4581119FB6483317B7A896A629F9E630` |
| FLV | 27.200500 秒 | 695,560 B | `378BEEEB31E2461CFC646164D8555820CF0C6EAC731F42C1D787F4CEDCF0B91A` |

- 原生记录 `20260907T181307262Z-missevan-native-integration.json`：公开 API、HLS、FLV 全部退出 0，总耗时 193.318 秒，峰值 CPU 9.73%、工作集 15,546,630,144 B，结束活跃重型进程数 0。成品路径/哈希与轨道索引见 `native-output-index.json`。
- 缓存原生 DLL `build/native_assets/windows/libffmpegkit.dll` 再次核对为 54,565,256 B / SHA-256 `888A15993862950FEED714990E459F653B68BBC2CE30DA7D4BDA8DB9A5212C06`。本轮构建钩子也报告远端 SHA256 验证通过，原生库版本为 `base-x86_64-lgpl-0.11.1-20260831`；没有替换 Root/LSP 或手机组件。

证据目录 `local-artifacts/missevan-integration-20260908/`，各轮资源记录在 `local-artifacts/build-records/`。原生探针使用注册工厂、生产 StreamResolver、请求头工厂、FFmpegCommandBuilder、FFmpegManager 和 MP4 转存服务；独立 FFprobe 检查轨道/时长，FFmpeg 对整个成品严格解码。外部检查子进程拥有超时、输出上限及失败终止路径。

探针为本机匿名直连 API/媒体，短片保存在忽略目录；不是应用设置代理、HLS relay、设备 UI 或长期续租证据。原生记录应包含输入协议、增长样本、停止、源分片提交后删除及成品轨道，区分各层结果，不用一段媒体外推全平台。

## 仍待完成

- Android 安装与首帧、16×16 视频/封面、前后台、音频模式、PiP；Windows 可见窗口、声音及长时运行。
- 应用完整录制会话的代理/relay 路径、长时签名刷新、纯音轨样本及不同直播房间；未实现搜索、弹幕、完整子分类合同。
- 新 Android/Windows 构建、完整质量门禁及最终发布。版本仍 3.1.8+4121，本批不操作手机、ADB、MT、Root 或 LSP。

回滚本批应用接入提交可恢复旧平台目录；收藏数据格式不变。旧版本会把不认识的猫耳从显示平台列表过滤，迁移值仍可能为 6，再升级后可能需手动启用猫耳；保留配置备份，不通过清空用户配置回滚。
