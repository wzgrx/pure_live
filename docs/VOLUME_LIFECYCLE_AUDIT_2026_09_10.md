# 音量初始化、事件与退出归属（2026-09-10）

## 范围与来源

基线 `9421c3aa6d5371bae8db93d46221fe0c9e192ee0`。根据 [Issue #858 审查](ISSUE_AUDIT_2026_09_10.md)继续代码优先排查，不操作手机。四项确定性失败来自实际 `VideoController`，只注入可控 `VolumeController` 接口替身；没有把音量业务复制到测试里。`dart:io` 平台分支在宿主上不随 TargetPlatformVariant 改变，因此新增窄的可选平台接口注入；默认生产 Android/iOS 路径仍使用原插件单例，桌面仍使用播放器音量。

来源 **upstream-existing（已核对冻结源码）**：本地冻结 `5ff565e9d21c79fa6e2b5f9434098de2cc940d4e`（liuchuancong，08-25）含相同的初始化、初始监听保存和 setter 等待后发布逻辑；维护分支随后沿用。此次未 fetch/合并或声称最新上游已修复，也未复现报告者 Android 16 的物理按键故障。下面是独立源码缺陷，不将它们等同于 #858 根因。

## 修改前的四项实际失败

基线只增加接口注入和用例，原处理顺序不变。记录 `local-artifacts/build-records/20260910T102429523Z-volume-lifecycle-baseline.json`：原有 5 项通过、新增 4 项失败。

| 第一处错误状态 | 最短时序 | 实际错误结果 |
| --- | --- | --- |
| 初始化读数后缺少退出检查 | getVolume pending → dispose → 返回 0.8 | 仍发出 `[1.0]` 的系统写入，本应不再调用 setter |
| 初始系统快照被当作用户变更保存 | 房间偏好 0.25 → 初始事件 0.8 → getVolume 返回 | 还原目标变成 0.8，而非 0.25 |
| 观察事件只持久化、不更新显示 Rx | 初始化后平台事件 0.65 | currentVolume 仍为 1.0 |
| setter 的迟到完成没有代次检查 | setVolume(0.3) pending → dispose → 完成 | 已退出控制器仍发布 0.3 到 Rx/房间偏好 |

初始事件时序依据锁定 `volume_controller 3.6.1` 的 Dart addListener 和 Android VolumeListener：请求初始值时 onListen 发出当前媒体音量。测试控制该交错，只证明应用处理合同；不证明 Android 实际事件顺序总是如此。

## 修订设计与相邻行为

- 保存房间目标音量后订阅，使用 `fetchInitialVolume=false`。显式读取当前系统值用于“已静音则保持静音”的判断，不再把初始快照持久化为房间偏好。
- 通过 `PlayerManager.ownsVideoController` 核对当前附着控制器，并保留 disposed 栅栏。旧房间初始化完成时不仅跳过音量写入，也跳过迟到的播放发起；旧控制器销毁不撤销新控制器的附着状态。
- 音量事件和 setter 建立修订代次；外部事件同步更新显示值与房间偏好，后来者优先。原生量化事件早于 setter 回包时，保留实际事件值而非覆盖为请求值。
- 读取后已退出返回 null；方向键遇到 null 直接结束，不再回落到最大音量；退休控制器也不再打开音量浮层。无穷/NaN 输入及事件不传播到设备或存储。
- 可选音量平台读写设置 2 秒等待上限并记录失败，初始平台错误或停滞不再无限阻塞播放发起。该上限**不取消已经提交的原生调用**，也不承诺设备音量物理回滚；迟到结果不再发布旧页面状态。
- 保留全局静音、设备原已静音、房间偏好、桌面播放器音量路径；没有修改系统按键分发、音频路由、插件/Root/LSP 版本、设置 schema 或翻译资源。

## 测试与结果

首轮修订为 **201 PASS / 2 FAIL**，记录 `20260910T102908700Z-volume-lifecycle-repair.json`。两个失败是新夹具把“音量预检完成且已发起播放”误断言成“已开始解码播放”；实际 fixture 不发出 playing 事件，loading 是预期状态。修订为直接断言 PlayerManager.play 调用一次，而非放宽播放器状态机；保留失败记录，未到达该轮请求的分析阶段。

最终五文件 **206/206 PASS**，新增 **16 项**（15 个音量/真实控制器用例、1 个键盘 Widget 用例）；五个修改 Dart 文件严格 `dart analyze --fatal-infos` 得到 **No issues found**。涵盖以上四项复现、旧/新房间交错、初始等待期间真实变更、量化事件、两次 setter 逆序完成、初始读取失败/停滞、已静音与全局静音、非法值、退休查询/控件、观察错误后继续收取事件，以及已有 source commit、音频模式/PiP、owned source 和错误恢复。

记录 `local-artifacts/build-records/20260910T103343238Z-volume-lifecycle-final.json`：165.546 秒，峰值 CPU 9.64%、工作集 15,630,913,536 B，结束活跃重型进程 0。日志在忽略目录 `local-artifacts/volume-lifecycle-20260910/`；记录 sourceCommit 是修改前的 9421c3aa，sourceFiles 哈希绑定实际未提交的三个业务文件与两个测试文件。最终提交前再次核对五个文件哈希与记录一致。

## 证据边界与下一步

- 206 项是相关回归，不是新一轮完整 4072 项套件/全部接口，也不是手机按钮、输出设备或听感验证。窗口中仍需普通页/全屏/前后台及实际媒体音量、显示值、物理按键分别对照。
- [48154d15 Android 候选](ANDROID_CUMULATIVE_CANDIDATE_2026_09_10.md)已验证且继续保留，**不含本次音量修订**。本轮未打新包、安装或发布；新业务源码须在后续候选/正式门禁按新 SHA 验证，不沿用旧 APK 宣称修复已上手机。
- 下一步继续检查 OverlayVolumeControl 在控制器替换时的订阅归属、迟到初始化与滑块/静音交互；目前只是一条未验证的相邻审查方向，不记为新发现已修复。
- 回滚可独立恢复本批三个业务文件及相应测试；没有数据迁移或用户数据清理。源码按每批验证后即时提交/push，3.2.0 仍待双端全面原生、长录、多路、参考平台扩展及全平台发布验收。历史 42 组未闭环和 7 组参考平台未注册的计数未因这些单元测试改变。
